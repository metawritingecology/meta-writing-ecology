# Synthetic fixture stub referenced by the manifest 'validator' artifact.
# Recorded in the inventory only as an existence check, never as generator identity.
raise RuntimeError("source-root validate_public_metadata.py must never be imported")
