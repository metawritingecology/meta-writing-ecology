# Agent Worklog

Agents must append entries here after making changes.

## Entry Format

### YYYY-MM-DD — agent-name — task-name

Agent:
Task:
Files changed:
Checks run:
Result:
Unresolved questions:
Risks or assumptions:

### 2026-07-06 — Claude Code — add-agent-coordination-rules

Agent: Claude Code
Task: Add repo-level agent coordination instructions so Claude Code, Codex, and GitHub Copilot do not conflict when working on this repository.
Files changed: AGENTS.md (new), CLAUDE.md (new), AGENT_TASKS.md (new), AGENT_WORKLOG.md (new), .github/copilot-instructions.md (new)
Checks run: Verified none of the five files existed prior to this change; confirmed no other repository files were modified.
Result: All five agent coordination files added as specified. No conceptual, model, relation-map, reading-path, public-anchor, or OSF-linked files were touched.
Unresolved questions: None.
Risks or assumptions: None — this was a pure addition of new instruction files with no changes to existing content.

### 2026-07-06 — Claude Code — integrate-five-osf-entries

Agent: Claude Code
Task: Integrate five prepared OSF-linked public entries (text-conditioned-semantic-rendering.md, surface-bounded-semantic-rendering.md, generation-condition-disclosure-reproducibility-cross.md, model-use-reporting-boundary-protocol.md, source-summary-citation-boundary-packet.md) into the public corpus and update navigation layers using the classification/placement mapping supplied by the user.
Files changed: text-conditioned-semantic-rendering.md (new), surface-bounded-semantic-rendering.md (new), generation-condition-disclosure-reproducibility-cross.md (new), model-use-reporting-boundary-protocol.md (new), source-summary-citation-boundary-packet.md (new), README.md, model-atlas/MODEL_ATLAS.md, model-atlas/RELATION_MAP.md, model-atlas/READING_PATHS.md, AGENT_WORKLOG.md
Checks run: `git status --short` and `git diff --stat` to confirm only the allowed/new files changed; confirmed the five prepared files were added verbatim without rewriting; confirmed all adjacent-entry filenames referenced in RELATION_MAP additions already exist in the repository.
Result: Five new entries added to the repository root exactly as prepared (no YAML front matter, no internal metadata added). README.md updated in Boundary Notes, Cross Structures, and Protocols/Method Orientations sections (plus the OSF/DOI Notes list). MODEL_ATLAS.md updated with entries under Boundary / Representation, Proxy / Legibility / Provenance, and AI-Readable Interface / Externalization, using navigation/public-source wording only. RELATION_MAP.md updated with navigation relations only — no relation was marked as a formal dependency, derived-from relation, parent-child relation, or confirmed ontology claim. READING_PATHS.md updated with two new reading paths (Semantic surface formation path; Source / reporting / reproducibility boundary path) and additions to the existing Boundary/representation path and AI/machine-reading path, per the user's explicit mapping.
Unresolved questions: None — placement, classification, and relation mapping were fully specified by the user in the task instructions.
Risks or assumptions: No public/private, naming, classification, or relation-validity judgments were made by Claude Code; all placements, adjacent-entry lists, and README/MODEL_ATLAS/RELATION_MAP/READING_PATHS wording followed the mapping the user supplied verbatim. No existing conceptual entry files were rewritten. No push or PR was made, per instructions.

### 2026-07-08 — Claude Code — readme-boundary-public-textual-projection

Agent: Claude Code
Task: Document the user-approved README boundary update, which reframed the README boundary wording as "public textual projection / not internal authorial operation layer."
Files changed: README.md (opening boundary paragraph only, in the approved commit); AGENT_WORKLOG.md (this entry).
Checks run: `git diff`, `git status --short`; ASCII not-equal marker scan (none); confirmed README.md was the only content file changed in the approved commit.
Result: README.md was the only content file changed in the approved commit. No model files, relation maps, OSF / DOI records, classifications, or registry authority logic were modified. No PR was opened. The metawritingecology-site repository was not modified.
Unresolved questions: None.
Risks or assumptions: Mechanical worklog entry documenting the already-approved README boundary refinement; no new conceptual claims added.

### 2026-07-13 — Codex — reading-path-boundary-routing

Agent: Codex
Task: Update reading paths so SUMMARY_BOUNDARIES.md is routed as a boundary/orientation layer without adding it to MODEL_ATLAS.md content, and remove numeric ordering from reading path headings because paths are not sequential reading instructions.
Files changed: model-atlas/READING_PATHS.md, AGENT_WORKLOG.md
Checks run: `python3` scripted edit; `git diff -- model-atlas/READING_PATHS.md`; `sed -n` review of AI-READING-GUIDE.md, SUMMARY_BOUNDARIES.md, public-anchors/ai-training-boundary-statement.md, and public-anchors/PUBLIC_ANCHOR_SCHEMA.md; local Markdown link check script; ASCII not-equal marker scan on touched human-facing files; `git diff --check`; `git status --short`.
Result: SUMMARY_BOUNDARIES.md was added to the first-time, AI/machine-reading, minimal, and dedicated summary/interpretation boundary paths. Reading path headings were made thematic rather than numbered. MODEL_ATLAS.md and RELATION_MAP.md were not changed.
Unresolved questions: None.
Risks or assumptions: SUMMARY_BOUNDARIES.md is treated as equal-to-or-higher-than atlas orientation rather than atlas content, per user instruction. No candidate/navigation relations were promoted and no conceptual model content was rewritten.

### 2026-07-13 — Codex — machine-reading-precedence-file

Agent: Codex
Task: Add a machine-reading precedence file and link it from public navigation surfaces per user approval.
Files changed: MACHINE_READING_PRECEDENCE.md (new), README.md, model-atlas/READING_PATHS.md, AGENT_WORKLOG.md
Checks run: local Markdown link check script; ASCII not-equal marker scan on touched human-facing files; `git diff --check`; `git status --short`.
Result: MACHINE_READING_PRECEDENCE.md was added as a public boundary guide. README.md and READING_PATHS.md now link to the new file, and READING_PATHS.md states that paths are thematic routes rather than required sequence, priority order, dependency chain, or ontology.
Unresolved questions: None.
Risks or assumptions: The new file is a boundary guide and does not promote candidate/navigation relations, expand the public corpus, or alter MODEL_ATLAS.md / RELATION_MAP.md content.

### 2026-07-13 — Codex — machine-reading-boundary-files

Agent: Codex
Task: Add user-approved machine-reading boundary files for relation status, summary constraints, and public-surface manifest.
Files changed: RELATION_STATUS_GUIDE.md (new), SUMMARY_CONTRACT.md (new), mwe-public-surface.json (new), README.md, MACHINE_READING_PRECEDENCE.md, model-atlas/READING_PATHS.md, AGENT_WORKLOG.md
Checks run: JSON validation; local Markdown link check script; ASCII not-equal marker scan on touched human-facing files; `git diff --check`; `git status --short`.
Result: Added the three approved files and linked them from public navigation surfaces.
Unresolved questions: None.
Risks or assumptions: The new files are public boundary and interpretation guides only; they do not confirm relations, expand the public corpus, or alter MODEL_ATLAS.md / RELATION_MAP.md content.

### 2026-07-13 — Codex — machine-interpretation-state-integration

Agent: Codex
Task: Add the approved machine interpretation state, inference ceiling, source-use routing, manifest integration, and selective early public-surface metadata for high-risk files.
Branch and initial HEAD: main at 5dd848ce1a49edf6ef44ce32a7fa36ceb5c84a2b.
Files added: MACHINE_INTERPRETATION_STATE.md, SOURCE_USE_GUIDE.md.
Files changed: README.md, MACHINE_READING_PRECEDENCE.md, AI-READING-GUIDE.md, SUMMARY_CONTRACT.md, mwe-public-surface.json, AGENT_WORKLOG.md, ai-induced-semantic-deviation.md, ai-readable-knowledge-architecture.md, model-induced-coherence-pressure.md, generation-condition-disclosure-reproducibility-cross.md, verification-labor-compression.md, surface-bounded-semantic-rendering.md, text-conditioned-semantic-rendering.md, model-use-reporting-boundary-protocol.md, policy-continuity-evidence-mapping.md, boundary-failure-diagnostics.md, premature-circulation-diagnostics.md, responsibility-alignment-diagnostics.md, semantic-field-diagnostics.md, constraint-residue-governance.md, source-summary-citation-boundary-packet.md, provenance-validity-separation-model.md, origin-control-validity-burden-accelerated-submission-systems.md, evaluation-boundary-failure-permitted-surface-variation.md.
Files audited but unchanged: None; all 18 listed high-risk files lacked equivalent early canonical machine-interpretation/source-use boundary links within the first 60 lines.
Checks run: `git status --short`; `git branch --show-current`; `git rev-parse HEAD`; complete read of required boundary/governance files; first-80-line and final boundary/naming-section audit of all 18 high-risk files; `git diff --check`; attempted `python -m json.tool mwe-public-surface.json` and `py -m json.tool mwe-public-surface.json` (Python unavailable on PATH); bundled Python `-m json.tool`; PowerShell `ConvertFrom-Json`; bundled Node JSON parse; canonical-entry existence check; duplicate JSON key scan; Markdown relative-link check; required cross-link check; touched Markdown literal `!=` scan; `git diff --name-only -- model-atlas public-anchors`; `git diff --summary`; high-risk `git diff --unified=0` review.
Result: Added repository-level machine interpretation state and source-use guides; integrated the manifest schema fields; linked the new guides from README, precedence, AI reading guide, summary contract, and manifest; added only compact early metadata blocks to high-risk files.
Unresolved questions: None.
Risks or assumptions: Python was unavailable on PATH, so JSON validation used the bundled Codex Python runtime, PowerShell, and the bundled Codex Node runtime. No classification, relation, naming, Registry, OSF-priority, semantic-supersession, or public/private decision was made. No commit, push, PR, release, model-atlas edit, public-anchor edit, conceptual-title change, DOI change, Naming Declaration change, or relation promotion was made.

### 2026-07-13 — Codex — public-metadata-and-misreading-register

Agent: Codex
Task: Add the public document metadata schema layer, selected public-document registry, public misreading/correction register, and local validator while preserving source and boundary authority.
Branch: codex/public-metadata-and-misreading-register.
Initial main SHA: dfe7dc9dc13a18d35b36b809ea1c1235864ecf2e.
Files added: mwe-document.schema.json, mwe-public-surface.schema.json, mwe-public-context.jsonld, mwe-public-documents.json, PUBLIC_MISREADING_REGISTER.md, public-misreading-register.json, public-misreading-register.schema.json, scripts/validate_public_metadata.py.
Files modified: README.md, MACHINE_INTERPRETATION_STATE.md, SOURCE_USE_GUIDE.md, MACHINE_READING_PRECEDENCE.md, AI-READING-GUIDE.md, mwe-public-surface.json, AGENT_WORKLOG.md.
Registry coverage count: 27 selected public documents.
Register initial entry count: 0.
Checks run: dependency check on main after `git fetch origin` and `git pull --ff-only origin main`; audited mwe-public-documents.json before creation; parsed JSON with bundled Python `-m json.tool`; checked registry record count, unique IDs, unique repository paths, and declared-classification source support; read back scripts/validate_public_metadata.py; AST function/import/prohibited-operation scan; attempted `python.exe scripts/validate_public_metadata.py` (failed because python.exe is unavailable on PATH); bundled Python `-m py_compile scripts/validate_public_metadata.py`; bundled Python `scripts/validate_public_metadata.py`; Markdown relative-link check; public metadata private-path scan; protected-path diff check; deleted-file check; `git diff --check`; touched prose literal `!=` scan.
Result: Added eight public metadata and correction-register files; integrated the manifest and public reading guides; validator passed with 27 registry records and 0 misreading cases. Protected files, conceptual source files, model-atlas files, public-anchor files, licenses, DOI fields, and OSF mirror content were not modified.
Unresolved questions: None.
Risks or assumptions: `python.exe` is unavailable on PATH in this shell, so the bundled Codex Python runtime was used for parse, compile, and validator execution after the exact requested command failed. No classification, relation, Registry, semantic-supersession, licensing, OSF-priority, DOI, training-permission, or public/private decision was made. The public registry is selected metadata only; the correction register is public correction status only.

### 2026-07-13 — Claude Code — public-surface-authority-map-prototype

Agent: Claude Code
Task: Add a local, reviewable D3 prototype "Public Surface and Authority-Ceiling Map" that visualizes the 27 selected public-document records with a deterministic transform script, from the merged main (PR #19).
Branch: claude/public-surface-authority-map-pr2weq (environment-designated; preferred name claude/public-surface-authority-map was pre-empted by the execution environment).
Initial main SHA: 729321914d190a9056b08dc72f3f23b21fc38540 (origin/main == git ls-remote == branch HEAD; PR #19 commit 68304c3 is an ancestor and all PR #19 files present).
Files added: scripts/build_public_surface_authority_map.py, visualizations/public-surface-authority-map/index.html, visualizations/public-surface-authority-map/app.js, visualizations/public-surface-authority-map/styles.css, visualizations/public-surface-authority-map/data.json, visualizations/public-surface-authority-map/README.md.
Files modified: AGENT_WORKLOG.md.
Source record count: 27. Generated node count: 27.
Generated edge count: 146 total — boundary_reference 120, source_use_reference 26. Self-reference edges omitted: 7. Every edge relation_status and authority_ceiling is navigation_only.
Checks run: python scripts/validate_public_metadata.py (passed, 27 records); python scripts/build_public_surface_authority_map.py (deterministic — running twice produced no diff); JSON integrity check (27 unique node ids/paths, every edge source/target is a node, no self-reference, only allowed edge types, no forbidden relation types); git diff --check (clean); protected-path diff checks for model-atlas, public-anchors, README.md and public metadata JSON/JSON-LD files (none changed); git ls-files --deleted (none); headless Chromium DOM smoke test over python -m http.server (27 node buttons, 6 surface_role groups, 6 legend items, 27 table body rows, status "27 of 27 records match current filters").
Result: Prototype directory and deterministic builder added. Data is generated only from mwe-public-documents.json (node source) and mwe-public-surface.json (scope confirmation); source metadata copied verbatim.
Unresolved questions: Two registry record names contain private-use-area Unicode characters (U+E634 in generation-condition-disclosure-reproducibility-cross.md; U+E638 in provenance-validity-separation-model.md). These were preserved verbatim and not repaired; flagged as a source-data observation for user review.
Risks or assumptions: D3 v7 (pinned d3@7.9.0) is loaded from a CDN; core rendering and the table fallback degrade gracefully if the CDN is unavailable and no D3 bundle is vendored. No browser-automation framework is installed, so interactive click/filter behavior was verified by static DOM dump and code review, not automated end-to-end. No commit, push, PR, release, route, or public-site change was made. No conceptual classification, relation confirmation, candidate-to-confirmed promotion, Registry status, semantic supersession, OSF priority, DOI, license, or public/private decision was made.

### 2026-07-13 — Claude Code — machine-reading-path-map-prototype

Agent: Claude Code
Task: Add a local, reviewable D3 prototype "Machine Reading Path and Inference-State Map" that simulates public-source reading-rule execution and the bounded claim the accessed evidence permits, from clean current main. Fresh implementation round; the public-surface-authority-map prototype was used only as an engineering/accessibility reference, not reused for graph semantics or data model.
Branch: claude/machine-reading-inference-map-99zwt3 (environment-designated; preferred name claude/machine-reading-path-map was pre-empted by the execution environment).
Initial main SHA: 18491105f0bc0451e0bf99eaa78c39f69c7cb57c (origin/main == git ls-remote == branch HEAD, 0/0 divergence; PR #20 public-surface-authority-map files all present).
Files added: scripts/validate_machine_reading_state_machine.py, visualizations/machine-reading-path-map/index.html, visualizations/machine-reading-path-map/app.js, visualizations/machine-reading-path-map/styles.css, visualizations/machine-reading-path-map/machine-reading-state-machine.json, visualizations/machine-reading-path-map/README.md, visualizations/machine-reading-path-map/examples/case-01-direct-source-classification.json, visualizations/machine-reading-path-map/examples/case-02-metadata-only.json, visualizations/machine-reading-path-map/examples/case-03-navigation-only.json, visualizations/machine-reading-path-map/examples/case-04-summary-only.json, visualizations/machine-reading-path-map/examples/case-05-version-unknown.json, visualizations/machine-reading-path-map/examples/case-06-classification-conflict.json.
Files modified: AGENT_WORKLOG.md.
Approved public semantic-source files used for grounding (exactly eight): MACHINE_INTERPRETATION_STATE.md, MACHINE_READING_PRECEDENCE.md, SOURCE_USE_GUIDE.md, SUMMARY_BOUNDARIES.md, SUMMARY_CONTRACT.md, RELATION_STATUS_GUIDE.md, AI-READING-GUIDE.md, mwe-public-surface.json. AGENTS.md, CLAUDE.md, and README.md are followed as repository-operation instructions only and are not semantic sources.
Rule count: 29 (8 claim-scope-selection, 11 uncertainty-derivation, 10 required-language). Source-rule count: 29. Transition count: 14. Stage count: 11. Example count: 6. Unsupported source-rule count: 0.
Narrow audit fix (Codex READY_AFTER_NON_BLOCKING_FIXES) applied to this same uncommitted change: (1) removed AGENTS.md, CLAUDE.md, and README.md from the approved semantic-source declaration in the state-machine JSON, README, and validator, so semantic grounding is limited to the eight approved public boundary/interpretation files; (2) removed the unsupported source rule SR030 (it cited AGENTS.md) and its references — T014 and the prohibited_reconstruction scope remain grounded by SR015 (MACHINE_INTERPRETATION_STATE.md, "Level 5 - Prohibited Automatic Reconstruction"), SR028 (mwe-public-surface.json, /is_full_archive), and SR029 (mwe-public-surface.json, /non_expansion_rule), so no replacement rule was invented and T014 is unchanged semantically; (3) added the explicit expected maximum_claim_scope = file_self_description to the case-06 fixture; (4) added a non-semantic horizontal-scroll affordance near the diagram and made the scroll container keyboard reachable (tabindex, role, accessible label). Source-rule count moved 30 -> 29; no transition behavior changed and no case-1..6 semantic result changed.
Single declarative rule source: machine-reading-state-machine.json is consumed by both the frontend (app.js) and the validator (scripts/validate_machine_reading_state_machine.py) via a generic table interpreter with no domain constants, so UI, validator, and examples cannot drift.
Validator commands: `python3 scripts/validate_public_metadata.py` (passed, 27 records); `python3 scripts/validate_machine_reading_state_machine.py` (passed, 322 checks, 6/6 examples, including the eight-file approved-source boundary check and the explicit case-06 maximum_claim_scope); JSON parse of the model and all six examples.
Browser test status: headless Chromium (pre-installed, CDP over Node built-in WebSocket; no browser framework installed) exercised the served page. With a local D3 copy (not committed): D3 loaded, 11 nodes / 13 edges rendered, all six example buttons produced the expected result states and scopes, a manual relation=conflicting change produced requires_authorial_confirmation, stage nodes were keyboard-focusable and Enter updated the stage detail, zero console errors. With the committed CDN import blocked in-sandbox: graceful degradation confirmed identical results via the plain-DOM fallback and full text summary, zero console errors.
Unresolved issues: The horizontal stage flow still scrolls inside its own container on the capped 1280px three-column layout; a visible scroll affordance and a keyboard-reachable scroll container were added, and the always-present textual rule-execution path lists the full sequence. prohibited_reconstruction is a defined-but-unreachable blocked category in this first prototype, which offers no reconstruction-request input.
Confirmation: no backend archive, private Registry file, prior ZIP archive, or unpublished material was read or used.
Confirmation: no classification, relation, candidate-to-confirmed promotion, Registry status, semantic-supersession, ontology, public/private, OSF-priority, DOI, license, or publication decision was made; no protected or conceptual file was changed; no commit, push, PR, route, release, or merge occurred.

### 2026-07-15 — Claude Code — public-surface-map-phase3b1-source-isolation

Agent: Claude Code
Task: Phase 3B-1 — add explicit source-root, isolated output, and a deterministic dependency inventory to the public-surface authority-map builder and validator, so an independently pinned generator checkout (G) can read a separately detached source checkout (S), write outside both roots, and emit an auditable read-dependency inventory, without changing map semantics or production output. Implementation only; no publication.
Phase 3A decision state: PHASE3A_PLAN_READY_WITH_USER_DECISIONS accepted; Option A (website-owned, manually gated generation) approved; complete generator-identity policy closure is the future target; explicit --source-root mandatory in candidate mode; approved generation-input status literal SOURCE_COMMIT_APPROVED_FOR_GENERATION (governance/records/tests only — not present in generated data, snapshots, metadata, UI, or source docs); Python standard library only; Phase 3B-1 precedes retention and workflow work.
Baseline SHA: origin/main == HEAD == 1e6b92f119eace061fe34c061d8f231543ae6e08 (matched the approved baseline; clean tree; no tracked deletion). Local branch claude/public-surface-phase3b1-source-isolation created from origin/main; not pushed.
Source/generator separation (S/G): source content and generator code are separate trust roots. In isolated mode all source reads resolve under an explicit --source-root; the builder uses __file__ only to identify the generator root and to reject output paths inside it; the source root is never inferred from __file__, CWD, output path, Git state, or environment.
Builder isolated interface: `--source-root`, `--output`, `--inventory-output` (all three required together); output and inventory must resolve outside both source root and generator root; absolute paths, parent traversal, and symlink/reparse escape fail closed; UTF-8/LF with one final LF; default no-argument mode is byte-for-byte backward compatible.
Validator isolated interfaces: `--source-root --mode preflight` (existing metadata + source-boundary validation against S; no inventory; no writes) and `--source-root --mode verify-inventory --inventory <file>` (existing validation plus inventory schema validation, identity recomputation, and path/purpose cross-check; --inventory required in verify-inventory, forbidden in preflight; no writes). Validator-order ambiguity resolved: preflight runs before any inventory exists; verify-inventory runs after generation.
No source-root Python execution: source-tree Python is treated only as data. Neither tool imports or executes any module discovered under S; the only generator-local module loaded is the companion builder, loaded by absolute path from the generator root. Isolated invocation and tests run with a working directory outside S, PYTHONPATH cleared, PYTHONNOUSERSITE=1, and S never on sys.path. A negative test plants poison json.py and build_public_surface_authority_map.py shadows in S and confirms a correct run never imports them.
Deterministic inventory contract: new mwe-public-surface-dependency-inventory.schema.json (JSON Schema Draft 2020-12, additionalProperties false, lowercase-only digest patterns, closed read-purpose enum). Top-level keys: inventory_schema_version, source_repository, interface_version, dependency_count, aggregate_sha256, files. Item keys: path (repo-relative forward-slash), byte_length, sha256, git_blob_sha1, read_purposes (unique, sorted). No timestamps, absolute paths, host/user names, run IDs, or generator identity. Inventory scope is the full builder+validator candidate-mode read/open/hash/existence-check union, derived from an instrumented trace of the unmodified tools: 36 entries. Read purposes: direct_input (1), scope_context (1), schema (3 files), registry_referenced_document (27), classification_evidence (12), reference_existence_check (references incl. manifest artifacts, canonical entries, @context, document_schema, register document, and the manifest-declared validator path — the latter recorded only as an existence check, never as generator identity). Two equivalent isolated runs produce byte-identical inventory bytes.
Fixture separation: minimal synthetic checked-in fixture tests/fixtures/public-surface-candidate/** (3 synthetic records; no production corpus copied) for CLI, isolation, inventory-schema, read-purpose coverage, and import-isolation unit tests; full production integration source materialised at test time from 18491105f0bc0451e0bf99eaa78c39f69c7cb57c into a temporary directory (never committed) for the exact-identity build.
Production output identity result: isolated build from detached 18491105 produced data.json = 83727 bytes, SHA-256 82f7f74b98a9b3b94a9ed0b12a394f1db2d9b5d256f700d311061c1353f4ef1e, Git blob aa25de9c60b0c0bcb2f8fec1f82bafc135e1f10b, 27 nodes, 146 edges — matching the approved identity exactly. Tracked production visualizations/public-surface-authority-map/data.json remains byte-identical (no repository diff).
Source and generator no-write result: source-root tree digest unchanged after successful generation and after representative failures (missing input, malformed metadata, rejected output paths); generator-root builder, validator, and data.json bytes unchanged after isolated runs.
Files modified: scripts/build_public_surface_authority_map.py, scripts/validate_public_metadata.py, visualizations/public-surface-authority-map/README.md, AGENT_WORKLOG.md.
Files added: mwe-public-surface-dependency-inventory.schema.json, tests/test_public_surface_candidate_generator.py, tests/fixtures/public-surface-candidate/** (13 synthetic files).
Checks run: `python3 scripts/validate_public_metadata.py` (passed, 27 records); `python3 scripts/build_public_surface_authority_map.py` (passed; deterministic; tracked data.json unchanged); `python3 -m unittest tests.test_public_surface_candidate_generator` (41 tests, 40 passed, 1 skipped — Windows-only reparse-point coverage reported as a platform limitation, POSIX symlink escape covered); instrumented access-trace of the unmodified builder+validator confirming the 36-entry union; `git diff --check` (clean); `git status --short`; `git diff --name-status`; `git ls-files --deleted` (none); touched-prose literal `!=` scan (none).
Provisional generator identities (PROVISIONAL_UNCOMMITTED_GENERATOR_IDENTITY — not approved, canonical, active, or production-ready; final identity requires a separately authorized commit/PR/merge and immutable commit/tree/blob ids): builder bytes=24581 SHA-256=50df56f4362dbb204a040816ef990a732661666f78f3228049e3cd467167509d Git-blob=d9b076ea8dca49ef07c95530c2d099e427c84c18; validator bytes=33759 SHA-256=5958df84cdd60a29e6b52a39cae8ff2582873b69a61364f804be14d622e18bbe Git-blob=4113ccae55267c86b1d7b604f03aeed66fadd824; inventory schema bytes=3891 SHA-256=3f1bc0b1983c5cc5d244e8de410be10487e59730be7da46115b6f995f8474c6a Git-blob=454bb77853b014166c191358d1b997b3795551c4; source-repository baseline 1e6b92f119eace061fe34c061d8f231543ae6e08; observed Python 3.11.15; dependency mode python_standard_library_only; proposed interface version 1.0.
Unresolved questions: Inventory coverage scope was resolved by the user to the full builder+validator candidate-mode access union (36 entries), grounded in an instrumented trace; recorded here as the boundary-sensitive decision for review.
Risks or assumptions: No source metadata, schema (other than the new inventory schema), conceptual Markdown, Registry content, public classification/relation record, boundary statement, navigation semantic, package manifest, lockfile, workflow, website file, or Cloudflare/deployment configuration was changed. Production data.json is byte-identical and no candidate snapshot, manifest movement, or publication was created. No network access, Git mutation, package installation, or dependency was introduced. No classification, relation confirmation, candidate-to-confirmed promotion, Registry status, public/private, OSF-priority, or naming decision was made. No commit, push, PR, merge, or deployment occurred.

### 2026-07-15 — Codex — phase3b1-drive-qualified-path-correction

Agent: Codex
Task: Apply the bounded Phase 3B-1 correction after the remote audit found that drive-qualified paths were accepted on non-Windows hosts because host-native `Path.is_absolute()` semantics were insufficient.
Correction: Aligned the inventory schema, builder source-path resolution, validator repository-path and inventory structural validation, and regression tests with a platform-independent drive-qualified-path rejection contract. The synthetic fixture count is 14; the prior count of 13 is superseded as a reporting correction.
Identity and scope: Production data identity is unchanged; the inventory remains the same 36-entry set; overall PR scope remains 20 paths.
Boundaries: No workflow, website change, publication, manifest movement, Phase 3B-2, merge, or deployment.

### 2026-07-18 — Codex + Claude Code — map-three-boundary-notes-27-to-30

Agent: Codex (Stage 1 planning and structural-review layer) prepared the bounded task package; Claude Code (bounded routine implementation layer) performed the mechanical file-level implementation only.
Task: Update the GitHub public-surface mapping to cover three existing root Markdown entries — delegated-execution-retained-answerability.md (Cross-Supporting Boundary Note, DOI 10.17605/OSF.IO/6M95U), structural-fidelity-use-validity-boundary.md (Cross-Supporting Boundary Note, DOI 10.17605/OSF.IO/BU4KT), and llm-condition-research-result-boundary.md (Protocol-Facing Boundary Note, DOI 10.17605/OSF.IO/47PXB) — across navigation, public metadata registry, generated authority-map data, validator/builder expectations, visualization totals, and the test suite.
Starting branch: claude/github-public-surface-mapping-sxknk1. Starting SHA: 89550fea8317e535f9569461e71fec8d46e9ad8e. Working tree reset to that baseline before implementation.
Old public registry count: 27. New public registry count: 30.
Files changed: README.md (Boundary Notes ×2, Protocols / Method Orientations ×1, OSF / DOI Notes ×3); model-atlas/MODEL_ATLAS.md (one navigation entry each under AI-Readable Interface / Externalization, Boundary / Representation, and Responsibility / Benefit-Burden / Cost); model-atlas/RELATION_MAP.md (three navigation-only rows appended); model-atlas/READING_PATHS.md (Responsibility / burden, Boundary / representation, Evaluation / boundary robustness, Provenance / validity, Protocols / method orientations, and Source / reporting / reproducibility boundary paths, with minimal purpose updates; no new path category); mwe-public-documents.json (record_count 27 → 30, three appended records with the optional doi field); scripts/validate_public_metadata.py (three added EXPECTED_REGISTRY_PATHS; expected total 27 → 30 with matching message); scripts/build_public_surface_authority_map.py (EXPECTED_RECORD_COUNT 27 → 30 and the "30 registry records" comment); visualizations/public-surface-authority-map/index.html (record totals 27 → 30); visualizations/public-surface-authority-map/README.md (operational totals 27 → 30); tests/test_public_surface_candidate_generator.py (DefaultModeTests kept on the working tree at 30 records; IntegrationTests refactored per option (c) — see below); AGENT_WORKLOG.md (this entry).
Generated file rebuilt: visualizations/public-surface-authority-map/data.json (rebuilt with the builder, not hand-edited).
RELATION_MAP adjacency sourcing: each new row's adjacent entries were taken only from that document's own "Relationship to Adjacent Public Frameworks" section. Delegated Execution → verification-labor-compression, responsibility-alignment-model, false-legibility, boundary-role-segmentation-model, benefit-burden-allocation-regimes. Structural Fidelity → provenance-validity-separation-model, proxy-substitution, false-legibility, observer-representation-boundary, boundary-failure-diagnostics, source-summary-citation-boundary-packet. LLM-Condition → generation-condition-disclosure-reproducibility-cross, model-use-reporting-boundary-protocol, provenance-validity-separation-model, verification-labor-compression, responsibility-alignment-model, ai-readable-knowledge-architecture, structural-fidelity-use-validity-boundary. No reciprocal rows were added; no adjacency was promoted to a confirmed relation, dependency, or ontology edge.
Generator totals: 30 nodes; 161 navigation-only edges (132 boundary_reference, 29 source_use_reference); 15 new edges (four boundary_reference plus one source_use_reference per new record); 7 self-reference edges omitted (unchanged; the three records introduce no self-reference); generated_record_count 30; two builder runs were byte-identical.
Generated identity (rebuilt LF-terminated data.json): byte length 92901; SHA-256 7cdc5441cb31b55427b0e4a2e271e5cc835f57b6668a7b7513240015c51bf33e; Git blob 3f5278fe2e097a9b3d3758db893c1cb49fc3e73a. Working-tree (DefaultModeTests) constants updated to this identity: EXPECTED_NODES 27 → 30; EXPECTED_EDGES 146 → 161; EXPECTED_INVENTORY_COUNT 36 → 39; EXPECTED_DATA_BYTES/SHA256/BLOB updated to the above.
Dependency inventory count: 39 (each new explicitly classified registry document adds one unique source dependency; a file carrying both registry-referenced-document and classification-evidence read purposes remains one inventory entry).
Integration-test resolution — option (c) (user-directed): The IntegrationTests class was refactored so the pinned historical commit is evaluated against its OWN pinned-commit state rather than against the working tree's 30-record builder expectation, with no working-tree overlay. Findings and implementation: (1) the former pin 18491105f0bc0451e0bf99eaa78c39f69c7cb57c predates isolated-candidate mode entirely — its builder is default-mode-only and it contains neither the inventory schema nor this test file — so its own tooling cannot exercise the isolated-mode integration assertions. (2) INTEGRATION_SHA was therefore repointed to the existing prepared clean-main commit 89550fea8317e535f9569461e71fec8d46e9ad8e, which carries a 27-record registry together with the full isolated-mode builder/validator (its own EXPECTED_RECORD_COUNT is 27); this is an existing commit, not a non-existent one. (3) IntegrationTests now materialise that commit into two separate temporary directories — a source extract and a clean generator extract — and run the generator extract's OWN builder/validator (via a new run_cli_from helper) against the source extract, so the pinned commit's 27-record expectation governs the build. (4) IntegrationTests use their own decoupled constants: INTEGRATION_EXPECTED_DATA_BYTES 83727, SHA-256 82f7f74b98a9b3b94a9ed0b12a394f1db2d9b5d256f700d311061c1353f4ef1e, Git blob aa25de9c60b0c0bcb2f8fec1f82bafc135e1f10b, 27 nodes, 146 edges, 36 inventory items — the pinned commit's own deterministic output, confirmed by rebuild. No working-tree mwe-public-documents.json was overlaid into the pinned checkout; INTEGRATION_SHA was not pointed at a non-existent commit; no assertion was relaxed. The integration test's purpose — verifying that a fixed historical repository state can be reconstructed and validated consistently — is preserved and now passes hermetically. DefaultModeTests continue to validate the current 30-record working tree.
Commands and checks run (PYTHONDONTWRITEBYTECODE=1): python scripts/validate_public_metadata.py (passed, 30 records) before and after the rebuild; python scripts/validate_machine_reading_state_machine.py (passed, 322 checks); python scripts/build_public_surface_authority_map.py run twice (30 nodes, 161 edges, deterministic — second run no diff); python -m unittest discover -s tests -p 'test_*.py' (45 tests: 44 passed, 0 failed, 1 skipped — Windows-only reparse-point coverage); DefaultModeTests 2/2 and IntegrationTests 14/14 confirmed individually; git diff --check (clean); git ls-files --deleted (none); protected-source diff on the three Markdown files (empty); mapping grep and DOI grep (6M95U/BU4KT/47PXB present in each source file, README, and the corresponding registry record); repository-relative link and filename-case audit; touched-prose ASCII not-equal scan (no new occurrences).
Link and DOI checks: all added repository-relative links resolve to existing root files with exact filename case; no broken source link was found or repaired; canonical public URLs use the exact root filenames.
Result: Three entries mapped consistently at a public registry count of 30 across README, MODEL_ATLAS, RELATION_MAP, READING_PATHS, the public registry, and the rebuilt authority-map data. No source Markdown body was modified. The full test suite passes with commit-pinned integration semantics preserved and no working-tree overlay.
Unresolved questions: None. Repointing INTEGRATION_SHA to the existing clean-main commit 89550fea and decoupling the integration constants were required to run the pinned commit's own isolated-mode tooling; both are recorded here for Codex review.
Risks or assumptions: Neither Codex nor Claude Code confirmed or changed internal Registry status, canonical classification, ontology, formal relation status, public/private authority, semantic supersession, or OSF priority. All classifications, adjacencies (taken from each file's own adjacency section), placements, and DOIs came from the source files and the user-approved package. Navigation relations, adjacencies, and reading paths remain navigation-only. No commit, push, branch, pull request, merge, publication, or deployment was performed; all changes are left uncommitted for Codex review.
Codex final-review mechanical corrections: After confirming that Option (c) correctly decoupled the working-tree and pinned historical identities, Codex found that the working-tree EXPECTED_NODES, EXPECTED_EDGES, and EXPECTED_INVENTORY_COUNT constants were declared but no longer referenced. The existing DefaultModeTests builder test now directly asserts 30 nodes, 161 edges, and 39 dependency-inventory entries in addition to the exact generated byte and SHA-256 identity. Codex also replaced two IntegrationTests calls to the working-tree builder's pure Git-blob helper with a test-local Git blob calculation, removing the final avoidable current-code dependency from historical identity checks. These corrections restore the stated current-working-tree coverage and make pinned historical assertions fully independent of working-tree builder functions without changing test count, generated identity, pinned builder/validator behavior, repository semantics, or authority boundaries.

### 2026-07-25 — Claude Code — phase3a-p0-historical-snapshot-test-identity-repair

Agent: Claude Code
Task: Phase 3A P0 historical snapshot test-identity repair. Correct three stale default-mode identity constants and one stale prose figure in tests/test_public_surface_candidate_generator.py so the test suite asserts the identity the tracked historical dataset already has, and close the declared-but-unasserted Git-blob gap.
Base: expected origin/main 3219fa03149b4bf1a229f059b4912b632028422b; resolved origin/main 3219fa03149b4bf1a229f059b4912b632028422b (no divergence).
Byte length: previous asserted EXPECTED_DATA_BYTES 92901; corrected to 92903 (the tracked artifact's real length).
SHA-256: previous asserted EXPECTED_DATA_SHA256 7cdc5441cb31b55427b0e4a2e271e5cc835f57b6668a7b7513240015c51bf33e; corrected to 3b1e5993a52cbce340b85472fea1ae5ea6f921cf8f7751d2d635edc7b17216ea.
Git blob: previous asserted EXPECTED_DATA_BLOB 3f5278fe2e097a9b3d3758db893c1cb49fc3e73a; corrected to 2d59c4fdd07a2a9ddfad94e2e214a2d1c84912af.
Prose correction: module docstring "exactly 92,901 bytes / 30 records" corrected to "exactly 92,903 bytes / 30 records". The docstring's "30 records / 161 edges / 39 inventory items" context line carries no byte figure and was left unchanged.
Assertion gap closed: EXPECTED_DATA_BLOB was declared but never referenced by any assertion. DefaultModeTests.test_default_builder_bytes_identical now asserts git_blob_sha1_bytes(produced) equals EXPECTED_DATA_BLOB, using the test module's existing pure Git-blob helper, immediately after the existing SHA-256 assertion. No existing assertion was weakened, replaced, or removed.
Unchanged constants: EXPECTED_NODES 30, EXPECTED_EDGES 161, EXPECTED_INVENTORY_COUNT 39 were already correct and were not touched. All INTEGRATION_* constants and INTEGRATION_SHA were not touched.
Root cause: commit 0479406 ("Repair public metadata title encoding", 2026-07-25) replaced two private-use-area characters with en dashes in two registry name values, each repair restoring one dropped character for exactly +2 bytes, and regenerated visualizations/public-surface-authority-map/data.json — without updating the three default-mode identity constants or the docstring byte figure. The tracked artifact is exactly what the current builder produces; the artifact was correct and the constants were stale.
Scope: this change repairs test identity only. No dataset, registry, schema, JSON-LD context, source concept Markdown, generator, navigation surface, or website content was changed. visualizations/public-surface-authority-map/data.json remains byte-identical at 92903 bytes, SHA-256 3b1e5993a52cbce340b85472fea1ae5ea6f921cf8f7751d2d635edc7b17216ea, Git blob 2d59c4fdd07a2a9ddfad94e2e214a2d1c84912af. The registry remains at 30 records.
Worklog gap recorded: commit 0479406 has no corresponding AGENT_WORKLOG.md entry. That fact is recorded here rather than by back-dating an entry into the worklog history.
Files changed: tests/test_public_surface_candidate_generator.py, AGENT_WORKLOG.md.
Checks run (all Python commands with PYTHONDONTWRITEBYTECODE=1): python3 -m unittest discover -s tests -p 'test_*.py'; python3 -m unittest tests.test_public_surface_candidate_generator.DefaultModeTests; python3 scripts/validate_public_metadata.py; git diff on visualizations/public-surface-authority-map/data.json (empty); wc -c, sha256sum and git hash-object on the tracked dataset (all three match the pinned identity); git diff --check; git ls-files --deleted (none); git status --porcelain (only the two allowed files); touched-prose ASCII not-equal scan (none).
Workspace gate: H0 was performed before this change. origin/main resolved to the expected SHA, git status --porcelain was empty, and no scripts/__pycache__ directory or .pyc file was present in this workspace, so no deletion was required.
Result: The default-mode identity constants now describe the tracked historical dataset exactly, and the Git-blob identity is enforced rather than merely declared.
Unresolved questions: None. Phase 3A P1 was not started, no evidence manifest was created, and the pull request is left unmerged for review.
Risks or assumptions: No MWE authority-level decision was made. No classification, relation status, candidate-to-confirmed promotion, Registry status, naming, public/private boundary, or OSF-priority decision was made or implied. No conceptual file was rewritten. The correction assumes only that the tracked artifact at the resolved base is authoritative and the test constants were stale, which was confirmed by direct measurement before editing.

### 2026-07-25 — Claude Code — phase3a-p1-public-document-registry-policy-and-evidence-contract

Agent: Claude Code
Phase: Phase 3A P1 — public-document registry policy and 30-record evidence contract.
Task: Establish the registry policy document and the parallel per-field evidence manifest, its schema, validator coverage and tests, while the selected public-document registry remains at exactly 30 records, so the evidence machinery is proven before any policy-assigned record is added.
Base: expected P1 base 6e5556485d257c66f0ce1260881282f57f0f798c (the P0 merge commit); resolved origin/main 6e5556485d257c66f0ce1260881282f57f0f798c (no divergence). Working tree clean before editing; full suite green before editing (45 tests, 0 failures, 1 platform skip).
Files added: PUBLIC_DOCUMENT_REGISTRY_POLICY.md, mwe-public-document-evidence.json, mwe-public-document-evidence.schema.json, tests/test_public_document_evidence.py.
Files modified: scripts/validate_public_metadata.py, AGENT_WORKLOG.md.
Registry unchanged: mwe-public-documents.json shows no diff against the P1 base. record_count remains 30, @graph remains 30 records, scope_note is unchanged, and no existing record object was touched. No record was added, removed or reordered.
Evidence manifest: mwe-public-document-evidence.json contains exactly 30 entries, one per current registry repository_path, in registry order, with record_count 30. Coverage against the registry is one-to-one with no duplicate, missing or extraneous path.
Evidence vocabulary: the closed nine-value vocabulary is source_declared, source_h1, source_declared_display_title, inventory_declared, mechanical, registry_policy, schema_const, not_asserted, user_decision. No synonym or additional status was introduced. Eleven fields are tracked per record: inclusion, name, repository_path, canonical_public_url, surface_role, public_surface_status, authority_ceiling, relation_default, classification, boundary_references, source_use_reference.
Inclusion evidence distribution: 23 inventory_declared (also declared by a "- **File:**" line in model-atlas/MODEL_ATLAS.md) and 7 registry_policy (the registry-only paths SUMMARY_BOUNDARIES.md, SUMMARY_CONTRACT.md, MACHINE_INTERPRETATION_STATE.md, SOURCE_USE_GUIDE.md, MACHINE_READING_PRECEDENCE.md, RELATION_STATUS_GUIDE.md, public-anchors/ai-training-boundary-statement.md).
Set identity recomputed at this base: |R| 30, |A| 52, |R and A| 23, |R or A| 59, |A minus R| 29, |R minus A| 7. R minus A reproduced exactly the seven registry-only paths. No record was added on the basis of this recomputation; it was used only to derive inclusion evidence for the existing 30.
Naming evidence distribution: 27 source_h1 and 3 source_declared_display_title. The three exceptions are delegated-execution-retained-answerability.md, structural-fidelity-use-validity-boundary.md and llm-condition-research-result-boundary.md, whose registry names were each confirmed byte-for-byte against that file's own declared OSF Project title, directly from the file and not inferred from MODEL_ATLAS.
Classification evidence distribution: 15 source_declared and 15 not_asserted, recomputed from the source files rather than assumed. The 15 source_declared records each carry a literal Classification: declaration; the other 15 carry none. This matches the registry's own classification_evidence field for all 30 records (15 explicit_in_file, 15 not_asserted), and the validator now enforces that agreement in both directions.
Public-surface-status and authority-ceiling evidence distribution: 21 source_declared and 9 registry_policy for each. The 21 are exactly the registered concept files whose own source carries the four-line public-surface header block, confirmed by a literal Public-surface status: declaration. The 9 non-concept records carry no such block and are recorded as registry-policy assignments.
Source-declaration versus registry-policy distinction: the manifest exists to make this asymmetry machine-visible. surface_role, relation_default and boundary_references are registry_policy for all 30, including records whose source text contains adjacent public-surface wording. public_surface_status and authority_ceiling are source transcriptions only where the source declares the block. repository_path and canonical_public_url are mechanical for all 30; source_use_reference is schema_const for all 30.
Boundary-reference evidence distribution: 30 registry_policy. No record's array is a complete source transcription. For the 21 concept files carrying the public-surface block, that block names 2 of the 4 references and the other 2 are policy-added, so the array is recorded as a registry-policy assignment. No mixed-evidence syntax was introduced. Recorded finding: README.md and MACHINE_READING_PRECEDENCE.md each contain all six of their listed boundary-reference filenames somewhere in their text, but only as navigation links and running mentions, not as boundary-reference declarations; a filename occurrence is not a declaration, so these remain registry_policy. The policy document states this rule explicitly.
Declaration-detection rule: a literal declaration is a header line in one of the three attested styles (bare "Key: value", bold list item "- **Key:** value", indented list item "* Key: value"). A prose mention is not a declaration. Recorded finding: MACHINE_INTERPRETATION_STATE.md and SOURCE_USE_GUIDE.md contain the phrase "Authority boundary:" in running text while carrying no public-surface header block, so authority-ceiling evidence is keyed to the presence of the public-surface block rather than to that phrase. A test asserts the pattern does not match a prose mention.
Validator: added EXPECTED_EVIDENCE_SCHEMA_VERSION = "1.0", validate_evidence_manifest(), validate_evidence_schema_file(), validate_evidence_against_source() and a shared source_declaration_pattern() helper; added both new files to the parsed-JSON list and the evidence schema to the schema-draft list; success output now reports both "registry records: 30" and "evidence records: 30". Checks enforced: Draft 2020-12 on the schema file, manifest parses, the eight required top-level keys, all four top-level constants, record_count equals the number of records, one-to-one registry coverage, no duplicate/missing/extraneous path, registry order, all eleven fields present, every value inside the closed vocabulary, evidence schema version 1.0, describes pointing at the registry, authority ceiling metadata_only, and the schema file still declaring the closed vocabulary and the eleven required fields so schema and validator cannot drift. Source cross-checks enforced in both directions for public_surface_status and for classification, plus naming-basis, mechanical-derivation and schema-constant support checks. The registry record-count gate, EXPECTED_REGISTRY_PATHS, registry classification behaviour and all generator logic were not changed.
Tests: added tests/test_public_document_evidence.py with 57 tests in seven classes, covering manifest count 30, identical registry and evidence path sets, path uniqueness, registry order, all eleven fields per entry, closed-vocabulary membership, source_declared public-surface claims supported by the file, registry_policy public-surface claims corresponding to absence, source_declared classification claims supported by a literal declaration, not_asserted classification claims corresponding to absence, inclusion agreement with the MODEL_ATLAS inventory, naming-basis agreement, registry envelope and path-set stability, the evidence files not being registered as public documents, historical dataset byte identity, policy-document coverage of the vocabulary and tracked fields, and 24 fail-closed rejection tests proving the contract rejects count mismatch, missing/extra/duplicate paths, out-of-vocabulary values, missing/untracked fields, unsupported source claims in either direction, mismatched authority-ceiling evidence, wrong constants, unknown/missing top-level fields, order divergence, registry disagreement, a widened vocabulary, an opened field-evidence object and a classification value added to the manifest schema. The historical-snapshot identity constants are imported from tests/test_public_surface_candidate_generator.py rather than restated, so the two suites cannot drift to different pinned values. No existing test was weakened, deleted or replaced, and no integration-pinned constant was modified.
Scope: no change to mwe-public-documents.json, mwe-document.schema.json, mwe-public-context.jsonld, mwe-public-surface.json, either surface schema, scripts/build_public_surface_authority_map.py, visualizations/public-surface-authority-map/data.json or its README, README.md, model-atlas/MODEL_ATLAS.md, model-atlas/RELATION_MAP.md, model-atlas/READING_PATHS.md, any source concept Markdown file, or any website-repository file. No DOI, version, license, date, OSF URL, abstract, source-commit or notes field was added. The policy document, evidence manifest and evidence schema were not registered as public-document records. No source header was normalized. No navigation surface was edited.
Historical artifact: visualizations/public-surface-authority-map/data.json remains byte-identical at 92903 bytes, SHA-256 3b1e5993a52cbce340b85472fea1ae5ea6f921cf8f7751d2d635edc7b17216ea, Git blob 2d59c4fdd07a2a9ddfad94e2e214a2d1c84912af, 30 nodes, 161 edges, 7 self-references omitted, 39 dependency-inventory entries.
Dependency inventory: unchanged at 39 entries. The builder derives the inventory from source metadata alone and P1 makes no generator change, so the two new metadata files are not enumerated there. Inventory-contract coverage of the evidence files is left to a later phase that is permitted to change the generator.
Checks run (all Python commands with PYTHONDONTWRITEBYTECODE=1): python3 -m unittest discover -s tests -p 'test_*.py' before editing (45 tests, 0 failures, 1 skip) and after (102 tests, 0 failures, 1 skip); python3 scripts/validate_public_metadata.py before and after (exit 0; after: registry records 30, evidence records 30); registry and evidence count probes (30 30 and 30 30); git diff against the P1 base on mwe-public-documents.json (empty); git diff on visualizations/public-surface-authority-map/data.json (empty); wc -c, sha256sum and git hash-object on the tracked dataset (all three match the pinned identity); dependency-inventory recomputation (39); git diff --check (clean); git ls-files --deleted (none); git status --porcelain (only the six allowed files); __pycache__ and *.pyc scan (none present); touched-prose ASCII not-equal scan on PUBLIC_DOCUMENT_REGISTRY_POLICY.md (none). All 17 ASCII not-equal markers added to scripts/validate_public_metadata.py are Python comparison operators and are preserved as code; the pre-existing occurrences in that script's error strings were left unchanged as executable-code content.
Result: The registry policy is documented and the 30-record evidence contract is adopted, validated and tested. Per-field provenance for the existing 30 records is now machine-visible, so the difference between a source transcription and a registry-policy assignment can be checked mechanically rather than inferred.
Unresolved questions: None blocking. Recorded for review: boundary-reference evidence is uniformly registry_policy under the approved no-mixed-syntax rule, which means the two document-declared references inside the 21 concept files are not separately visible at field level; raising that would require a mixed-evidence syntax, which P1 deliberately does not introduce. Optional fields permitted by mwe-document.schema.json are untracked at evidence schema version 1.0, including the doi value carried by three records.
Risks or assumptions: No MWE authority-level decision was made. No classification, relation status, candidate-to-confirmed promotion, Registry status, naming, public/private boundary, ontology, semantic supersession or OSF-priority decision was made or implied. No conceptual file was rewritten. Every evidence value was derived from the source file, the registry structure, the MODEL_ATLAS inventory declaration, or the document schema, and nothing was guessed; where a claim could not have been proven from the file the task was to stop rather than downgrade, and no such case arose. Phase 3A P2 was not started, the historical generator was not isolated or redesigned, the 29 proposed records were not added, the registry was not expanded to 59, and the pull request is left unmerged for review.

### 2026-07-25 — Claude Code — phase3a-p2-historical-generator-isolation

Agent: Claude Code
Phase: Phase 3A P2 — historical generator isolation.
Task: Remove the ability of current or future live-registry generation to overwrite visualizations/public-surface-authority-map/data.json. The historical artifact becomes immutable and verify-only; expanded generation becomes a separate target with an explicit non-historical output path.
Base: expected P2 base 614f5133285e38286f921140b6990824398fb52e (the PR #26 merge commit); resolved origin/main 614f5133285e38286f921140b6990824398fb52e (no divergence). Branch: claude/phase-3a-p2-isolation-ddyy6b (session-designated). Working tree clean before editing; baseline green before editing (validator exit 0 with registry records 30 and evidence records 30; full suite 102 tests, 0 failures, 1 platform skip; generator module 45 tests green); historical identity re-confirmed before editing at 92903 bytes, SHA-256 3b1e5993a52cbce340b85472fea1ae5ea6f921cf8f7751d2d635edc7b17216ea, Git blob 2d59c4fdd07a2a9ddfad94e2e214a2d1c84912af, empty git diff.
Files changed: scripts/build_public_surface_authority_map.py, tests/test_public_surface_candidate_generator.py, visualizations/public-surface-authority-map/README.md, AGENT_WORKLOG.md (this entry). No other tracked file changed. No test module was added.
CLI behaviour before: zero-argument invocation was the default mode; it read the two approved inputs from the generator root and wrote the tracked data.json in place. That was the single point of exposure for the historical artifact. Isolated candidate mode (--source-root / --output / --inventory-output) already existed and already refused to write inside either root.
CLI behaviour after: zero-argument invocation is verify-only. It reads the tracked artifact, computes its byte length, SHA-256 and Git blob identity, inspects its structural invariants, compares all values with the pinned historical specification, exits 0 only when every check matches, and writes nothing. It does not rebuild data.json, open it for writing, replace it temporarily, restore it after a run, alter its modification time, create an inventory beside it, or generate any other persistent artifact.
Historical target: --target historical performs the same verify-only operation, made explicit. It is not permitted to regenerate the artifact and accepts no output-bearing flag; --output, --source-root or --inventory-output alongside it fails closed.
Expanded target: --target expanded --output <path> is a separate generation target. --output is mandatory, its expected record count is 59, and no expanded output path is selected implicitly. At this base the live registry still contains 30 records, so a non-colliding expanded invocation stops on the existing record-count protection; that behaviour was kept rather than reducing the expected count to make it succeed. No expanded dataset was generated, committed or retained, and successful expanded generation remains deferred to the later authorized visualization phase.
Isolated candidate mode: unchanged. --source-root, --output and --inventory-output are still required together and still resolve exactly as before, at 30 records. Source content still resolves only within the explicit source root; output and inventory still resolve outside both the source root and the generator root; absolute-path misuse, parent traversal and symlink escape still fail closed. No existing safety rule was removed or weakened.
Per-target constants: the module-global coupling between one record count and one output path was replaced by an explicit per-target specification. The historical target owns visualizations/public-surface-authority-map/data.json at 30 records in verify-only mode; the expanded target carries 59 records and deliberately has no output-path constant at all. assemble_map_data and build_nodes now take the calling target's expected record count instead of reading a module global. No count or path assertion was weakened.
Path-collision guard: an expanded output whose resolved path equals the resolved historical artifact path fails closed before any generation or record-count processing, so a collision can never reach a write. Resolved paths are compared rather than raw strings, so relative, absolute, parent-traversing and symlinked aliases all collapse to the same real location and are rejected identically.
Failure tokens: HISTORICAL_ARTIFACT_IDENTITY_MISMATCH (emitted when the tracked artifact does not match its pinned identity, or is missing or unreadable), HISTORICAL_OUTPUT_PATH_COLLISION (emitted when an expanded output resolves to the historical artifact path), EXPANDED_TARGET_REQUIRES_OUTPUT (emitted when the expanded target is invoked without --output). All three are asserted by tests and are shared constants, so the builder and the test module cannot drift to different strings.
No-write proof: a test captures raw bytes, byte length, SHA-256, Git blob and stat().st_mtime_ns before and after both the zero-argument invocation and --target historical, and requires all five to match. A further test confirms the artifact's directory listing is unchanged, so no sibling or temporary replacement file is created. Every path-collision case repeats the byte, mtime and directory-listing checks.
Test migration: the former default-mode builder test generated into the tracked historical path and restored it afterwards. That behaviour is retired and the test is now a verification test that never writes. All existing assertion intent is preserved — 92903 bytes, SHA-256 identity, Git blob identity, 30 nodes, 161 edges, 39 dependency-inventory entries — and the three further artifact-carried invariants were added: 132 boundary-reference edges, 29 source-use-reference edges, 7 self-references omitted. No assertion was removed or weakened because the execution path changed. The historical identity-mismatch path is proven by dependency injection against an isolated temporary copy; the tracked artifact is never edited and never restored as part of any test. Suite grew from 102 to 121 tests (generator module 45 to 64), 0 failures, 1 platform skip.
Integration tests: INTEGRATION_SHA, every INTEGRATION_EXPECTED_* constant, the IntegrationTests fixtures and the pinned reconstruction identity are unchanged. IntegrationTests continue to run the pinned commit's own tooling and remain independent of the new current-tree target logic.
Pinned source commit: 3219fa03149b4bf1a229f059b4912b632028422b.
Reconstruction result: git archive of the pinned commit into a clean temporary directory, built by the current tree's builder in isolated mode with output outside both roots, produced 92903 bytes, SHA-256 3b1e5993a52cbce340b85472fea1ae5ea6f921cf8f7751d2d635edc7b17216ea, Git blob 2d59c4fdd07a2a9ddfad94e2e214a2d1c84912af, 30 nodes, 161 edges and 39 dependency-inventory entries. cmp against the tracked artifact reported byte-identical. Both temporary directories were created fresh and removed after verification.
Historical identity after the change: 92903 bytes, SHA-256 3b1e5993a52cbce340b85472fea1ae5ea6f921cf8f7751d2d635edc7b17216ea, Git blob 2d59c4fdd07a2a9ddfad94e2e214a2d1c84912af, 30 nodes, 161 edges, 132 boundary-reference edges, 29 source-use-reference edges, 7 self-references omitted, 39 dependency-inventory entries. git diff on the artifact is empty.
Dependency-inventory count: the pinned value 39 is checked by the reconstruction and by the retained test assertion, and is pinned and reported by the verify-only mode. It is deliberately not recomputed from live registry state during verification, because it is a provenance fact of the pinned source commit rather than a property of the artifact bytes; making verification depend on the live registry would reintroduce exactly the coupling this phase removes. Recorded for review.
Scope: registry remains 30 records and evidence remains 30 records. No change to mwe-public-documents.json, mwe-public-document-evidence.json, mwe-public-document-evidence.schema.json, mwe-document.schema.json, mwe-public-context.jsonld, mwe-public-surface.json, either surface schema, PUBLIC_DOCUMENT_REGISTRY_POLICY.md, visualizations/public-surface-authority-map/data.json, README.md, model-atlas/MODEL_ATLAS.md, model-atlas/RELATION_MAP.md, model-atlas/READING_PATHS.md, any source concept Markdown file, or any website-repository file. The 29 proposed records were not added, the registry was not expanded, the evidence manifest was not expanded, no expanded dataset was generated or committed, no source header was normalized, and no navigation surface was edited.
Checks run (all Python commands with PYTHONDONTWRITEBYTECODE=1): zero-argument builder (exit 0, verify-only, no write); builder --target historical (exit 0, verify-only, no write); python3 -m unittest tests.test_public_surface_candidate_generator (64 tests, 0 failures, 1 skip); python3 -m unittest discover -s tests -p 'test_*.py' before editing (102 tests, 0 failures, 1 skip) and after (121 tests, 0 failures, 1 skip); python3 scripts/validate_public_metadata.py (exit 0; registry records 30, evidence records 30); git diff on the historical artifact (empty); wc -c, sha256sum and git hash-object on the artifact (all three match the pinned identity); byte and mtime comparison across every verify and collision invocation (unchanged); git diff against the P2 base on mwe-public-documents.json, mwe-public-document-evidence.json, mwe-public-document-evidence.schema.json, mwe-document.schema.json and mwe-public-context.jsonld (all empty); Git-pinned reconstruction with cmp against the tracked artifact (byte-identical); expanded-target collision rejection for the exact relative path, an absolute path, a parent-traversing path and a symlink alias (all rejected, artifact unchanged, nothing left behind); git diff --check (clean); git ls-files --deleted (none); git status --porcelain (only the four allowed files); __pycache__ and *.pyc scan (none present); touched-prose ASCII not-equal scan on visualizations/public-surface-authority-map/README.md (none). All ASCII not-equal markers in scripts/build_public_surface_authority_map.py are Python comparison operators and are preserved as code.
Result: The historical 30-node artifact is immutable and verify-only. No zero-argument or explicit-historical invocation can write it, and no expanded output can resolve to its path. The registry expansion planned for P3 will land against a generator that is provably incapable of overwriting the artifact.
Unresolved questions: None blocking. Recorded for review: the dependency-inventory count of 39 is pinned and reported rather than recomputed during verification, for the reason stated above. Successful expanded generation is untested end to end at this phase because the live registry carries 30 records, not 59; the expanded target's record-count protection is exercised instead, and generation is deferred to the later authorized visualization phase.
Risks or assumptions: No MWE authority-level decision was made. No classification, relation status, candidate-to-confirmed promotion, Registry status, naming, public/private boundary, ontology, semantic supersession, visualization-membership or OSF-priority decision was made or implied. No conceptual file was rewritten. The pinned historical values were treated as authoritative and were never updated to accommodate an unexpected file; an identity mismatch is handled as a failure, not a regeneration request. Phase 3A P3 was not started, the registry was not expanded to 59, and the pull request is left as a draft and unmerged for review.

### 2026-07-25 — Claude Code — phase3a-p2-review-follow-up-isolation-gaps

Agent: Claude Code
Phase: Phase 3A P2 — review follow-up on PR #27 (historical generator isolation). Narrowly scoped correction of three review blockers. No redesign of the generator and no revision of the approved P2 architecture.
Base and head: base unchanged at 614f5133285e38286f921140b6990824398fb52e; previous head f6f3250ec5c023a46ba05b35e0ae126216407f32 on branch claude/phase-3a-p2-isolation-ddyy6b. The existing three commits were not amended, rebased or rewritten; this is one additional commit on the same branch and the same pull request.
Files changed: scripts/build_public_surface_authority_map.py, tests/test_public_surface_candidate_generator.py, visualizations/public-surface-authority-map/README.md, AGENT_WORKLOG.md (this entry). No other tracked file changed. No test module was added.
Blocker 1 — live-registry coupling removed from historical tests: the current-tree historical test previously called builder.build_dependency_inventory(GENERATOR_ROOT.resolve()) and asserted dependency_count 39. That recomputed the inventory from the live working tree and coupled historical verification to live registry contents, which would break once P3 expands the registry to 59 records without being permitted to touch P2 files. The live-root recomputation was removed from that test. The 39-entry assertion was not removed: it was relocated to a new HistoricalReconstructionTests class that materialises source commit 3219fa03149b4bf1a229f059b4912b632028422b into a clean temporary source root, runs the current tree's isolated candidate mode with output and inventory written only to temporary paths outside both roots, and asserts 92903 bytes, SHA-256 3b1e5993a52cbce340b85472fea1ae5ea6f921cf8f7751d2d635edc7b17216ea, Git blob 2d59c4fdd07a2a9ddfad94e2e214a2d1c84912af, 30 nodes, 161 edges and 39 dependency-inventory entries, then confirms the reconstruction is byte-identical to the tracked artifact and removes its temporary directory. A second test in that class confirms the reconstruction leaves the tracked artifact's bytes and mtime unchanged.
Blocker 1 — regression coverage: a new HistoricalLiveRegistryIsolationTests class proves the dependency does not exist rather than asserting it will not matter. test_verification_never_builds_the_live_inventory patches build_dependency_inventory, collect_read_purposes, assemble_map_data and load_json to raise if called, and requires run_historical() to still exit 0. test_cli_verifies_without_any_registry_present builds an isolated temporary generator root containing only the builder script and a copy of the artifact, with no mwe-public-documents.json and no mwe-public-surface.json, and requires both the zero-argument invocation and --target historical to exit 0, which is only possible if neither file is read. test_cli_verifies_against_a_divergent_registry repeats that with a registry declaring 59 records and an empty graph, and requires verification to remain indifferent to it. The live registry was not modified to simulate 59 records. The historical zero-argument tests assert only that the CLI reports the pinned value 39; they do not prove it by reading the live registry.
Blocker 2 — unreadable artifact fails closed: run_historical() previously called path.read_bytes() directly, so a file that exists but cannot be read could raise PermissionError or another OSError and produce a traceback instead of the stable token. The read is now wrapped in a narrow OSError handler that fails with HISTORICAL_ARTIFACT_IDENTITY_MISMATCH and reports the underlying read failure, then passes the bytes to historical_identity_mismatches(). The artifact is still only ever opened for reading and no regeneration can occur. Two tests were added: test_permission_error_emits_failure_token injects PermissionError(EACCES) at the read call through mocking against an injected temporary artifact path, and test_os_error_on_read_emits_failure_token injects OSError(EIO). Both require a non-zero exit, the stable token in stderr, the read failure described, and the tracked artifact's bytes and st_mtime_ns unchanged. No permission on the tracked artifact was altered. The pre-existing invalid-JSON case was retained as a separate structural-readability test, renamed test_structurally_unreadable_artifact_emits_failure_token.
Blocker 3 — hard-link aliases rejected: Path.resolve() detects relative, absolute, parent-traversing and symlinked aliases, but a hard link is a distinct pathname that resolves to itself while sharing an inode with the artifact, so writing through it would mutate the artifact. resolve_expanded_output() now applies two independent checks: resolved-path equality, and, for an output path that already exists, same-file identity via Path.samefile (device and inode). If the candidate exists but its identity cannot be established, the run fails closed rather than allowing an alias that cannot be ruled out to reach a write. Both checks run before reading the live registry, before the expanded record-count check, before assembling data, before any parent-directory creation and before any file is opened. The failure message distinguishes the three cases (resolves to, is a hard link to, cannot be distinguished from) while emitting the same HISTORICAL_OUTPUT_PATH_COLLISION token.
Blocker 3 — hard-link tests: test_hard_link_alias_of_historical_path_rejected creates a real hard link to the tracked artifact outside the repository with os.link, confirms it is not a symlink, that its resolved path differs from the artifact's, and that samefile reports identity, then requires a non-zero exit, the collision token, unchanged artifact bytes and st_mtime_ns, unchanged link bytes, an unchanged directory listing beside the artifact, and that the expanded target's 59-record error never appears, proving rejection precedes record-count processing. test_hard_link_guard_is_same_file_identity repeats the check at helper level. The link directory falls back from the shared temporary root to a directory on the same filesystem as the repository, so a cross-device temporary directory cannot turn a real gap into a silent skip; the test skips only when os.link is unavailable or the filesystem reports a documented unsupported error. test_ordinary_existing_output_is_not_a_collision confirms the new same-file check does not reject an unrelated existing file, which still proceeds to the record-count guard. All four pre-existing collision tests were retained unweakened: exact relative path, absolute path, parent traversal and symlink alias.
Tests: generator module grew from 64 to 74 tests; full suite from 121 to 131 tests. 0 failures, 1 platform skip (the pre-existing Windows-only reparse-point note). No existing assertion was weakened or removed. INTEGRATION_SHA, every INTEGRATION_EXPECTED_* constant and the IntegrationTests fixtures are unchanged; materialise_integration_source now delegates to a shared materialise_commit helper with identical behaviour.
README: the Historical artifact section now lists all five covered alias forms (relative path, absolute path, parent traversal, symlink alias, hard-link alias), states that resolved-path equality is supplemented by same-file identity checking for existing paths, and records that an unreadable file fails closed with the stable token. The historical identity values and the reconstruction command are unchanged.
Checks run (all Python commands with PYTHONDONTWRITEBYTECODE=1): zero-argument builder (exit 0, verify-only, no write); builder --target historical (exit 0, verify-only, no write); python3 -m unittest tests.test_public_surface_candidate_generator (74 tests, 0 failures, 1 skip); python3 -m unittest discover -s tests -p 'test_*.py' (131 tests, 0 failures, 1 skip); python3 scripts/validate_public_metadata.py (exit 0; registry records 30, evidence records 30); targeted verbose runs of HistoricalLiveRegistryIsolationTests, HistoricalReconstructionTests, HistoricalIdentityFailureTests and ExpandedTargetTests (23 tests, all ok, none skipped); stat, wc -c, sha256sum and git hash-object on the tracked artifact before and after all validation (unchanged); git diff against the P2 base on the historical dataset, mwe-public-documents.json, mwe-public-document-evidence.json, mwe-public-document-evidence.schema.json, mwe-document.schema.json and mwe-public-context.jsonld (all empty); fresh pinned-source reconstruction with cmp against the tracked artifact (byte-identical, 92903 bytes, 30 nodes, 161 edges, 39 inventory entries, temporary directories removed); direct CLI checks of hard-link, symlink and ordinary-existing-file outputs (hard link and symlink rejected with the token, ordinary file reaching the record-count guard); git diff --check (clean); git ls-files --deleted (none); git status --porcelain (only the four allowed files); __pycache__ and *.pyc scan (none present); touched-prose ASCII not-equal scan on visualizations/public-surface-authority-map/README.md (none). All ASCII not-equal markers in scripts/build_public_surface_authority_map.py remain Python comparison operators.
Historical artifact: unchanged at 92903 bytes, SHA-256 3b1e5993a52cbce340b85472fea1ae5ea6f921cf8f7751d2d635edc7b17216ea, Git blob 2d59c4fdd07a2a9ddfad94e2e214a2d1c84912af, 30 nodes, 161 edges, 132 boundary-reference edges, 29 source-use-reference edges, 7 self-references omitted. Its mtime was unchanged across every verification and collision test.
Scope: registry remains 30 records and evidence remains 30 records. No change to any registry, evidence, schema, JSON-LD, validator, navigation-surface, source concept Markdown or website-repository file. No expanded artifact was generated or committed. No P3 work was introduced.
Result: The three review blockers are closed. Historical verification now reads the tracked artifact and nothing else, so it remains correct across the P3 registry expansion; an unreadable artifact fails closed with the stable token instead of raising; and a hard-link alias can no longer reach expanded generation.
Unresolved questions: None blocking. Recorded for review: the same-file check applies only to an output path that already exists, since a non-existent path cannot share an inode with the artifact; a path created between the check and the write is outside the scope of a single-process guard and is not addressed here.
Risks or assumptions: No MWE authority-level decision was made. No classification, relation status, candidate-to-confirmed promotion, Registry status, naming, public/private boundary, ontology, semantic supersession, visualization-membership or OSF-priority decision was made or implied. No conceptual file was rewritten. The pinned historical values were not changed. Phase 3A P3 was not started, and pull request #27 remains a draft and unmerged for review.

### 2026-07-25 — Claude Code — phase3a-p3-public-document-registry-expansion-30-to-59

Agent: Claude Code
Phase: Phase 3A P3 — public-document registry expansion from 30 to 59 records and evidence-manifest expansion from 30 to 59 entries. Implementation of the approved task package only. No conceptual file was rewritten and no authority-level decision was made.
Base and branch: expected P3 base 7a5e5fe59203cc7de6e70c3d4080bbf3b92c9008, the merge commit of pull request #27; resolved origin/main matched it exactly, so no base divergence handling was required. Work was carried out on the session-designated branch claude/phase-3a-registry-expansion-y4ds9b, created from that commit with a clean working tree. P2 history was not amended, rebased or rewritten.
Files changed: mwe-public-documents.json, mwe-public-document-evidence.json, scripts/validate_public_metadata.py, tests/test_public_document_evidence.py, tests/test_public_surface_candidate_generator.py, AGENT_WORKLOG.md (this entry). No other tracked file changed. No test module was added.
Counts: registry record_count 30 to 59 and @graph length 30 to 59; evidence record_count 30 to 59 and records length 30 to 59.
Candidate set recomputed at the P3 base, not reused from planning: R (repository_path values in mwe-public-documents.json @graph) = 30; A (files declared by a literal "- **File:**" line in model-atlas/MODEL_ATLAS.md) = 52; R intersect A = 23; R union A = 59; A minus R = 29; R minus A = 7. All 59 union paths exist as regular files, all are unique, and the 29 additions reproduced the approved appendix enumeration in exactly the appendix order, which is MODEL_ATLAS declaration order. The seven registry-only paths (SUMMARY_BOUNDARIES.md, SUMMARY_CONTRACT.md, MACHINE_INTERPRETATION_STATE.md, SOURCE_USE_GUIDE.md, MACHINE_READING_PRECEDENCE.md, RELATION_STATUS_GUIDE.md, public-anchors/ai-training-boundary-statement.md) were untouched.
The 29 additions, in append order: AUTHOR.md, semantic-cyberpunk-condition.md, cultural-curvature-unified-field.md, irreversibility-conditions.md, semantic-curvature-dynamics.md, semantic-curvature.md, semantic-physics.md, semantic-pressure.md, semantic-propagation-mechanics.md, semantic-virology.md, zero-field.md, boundary-engineering.md, boundary-failure.md, boundary-integration-failure.md, boundary-role-segmentation-model.md, observer-representation-boundary.md, false-legibility.md, proxy-substitution.md, premature-circulation-model.md, premature-coherence.md, reality-consistency.md, reference-drift.md, constraint-displacement.md, constraint-residue-accumulation-model.md, high-integrity-system-architecture.md, benefit-burden-allocation-regimes.md, cost-visibility-redistribution.md, external-lifeline-collapse-under-residual-infrastructure-cross.md, responsibility-alignment-model.md.
Both required set corrections hold and are asserted by test: cultural-curvature-unified-field.md is one of the 29 additions (position 3); constraint-residue-governance.md is not an addition, because it was already registry record #23 and is therefore in R intersect A. Both are proved against the pre-expansion registry read at the P3 base commit through Git, not against the working tree.
AUTHOR.md treatment, exactly as approved and not reopened: surface_role repository_orientation, @type schema:DigitalDocument, public_surface_status public_navigation_surface, authority_ceiling navigation_only, relation_default navigation_only, classification_evidence not_asserted, orientation 6-set boundary references, name "Author" from the source H1. It is registered but is not a concept node, receives no declared classification, and no field was inferred from MODEL_ATLAS Type. A test asserts each of these values and that its surface_role is not concept_node.
Concept treatment for the other 28 additions: surface_role concept_node, @type schema:CreativeWork, public_surface_status selected_external_node, authority_ceiling public_file_claim_only, relation_default adjacency_only, concept 4-set boundary references, source_use_reference SOURCE_USE_GUIDE.md, name the verbatim source H1. Classification remained fail-closed and was not inferred from filename, H1 wording, MODEL_ATLAS Type, MODEL_ATLAS field section, MODEL_ATLAS declared function, RELATION_MAP adjacency, title similarity, relation density or document length.
Sole classified addition: external-lifeline-collapse-under-residual-infrastructure-cross.md, with classification_evidence explicit_in_file and publicly_declared_classification "Cross / Structural Account / Domain Declaration". The literal was copied byte-verbatim from source line 8, which reads "- **Classification:** Cross / Structural Account / Domain Declaration", well within the first 80 source lines. That literal is already present in the registry via generation-condition-disclosure-reproducibility-cross.md, so no new classification literal was introduced; the set of distinct declared classification literals remains 12. The other 28 additions carry classification_evidence not_asserted and no publicly_declared_classification field.
Final classification split: 16 explicit_in_file and 43 not_asserted across the 59 records, recomputed from the sources rather than assumed. No data was adjusted to reach that split.
Original 30 registry record objects unchanged: a canonical serialization of @graph[0:30] was extracted from the P3 base commit before editing and compared with the same extraction from the working tree after implementation; the two are byte-identical. The raw diff on mwe-public-documents.json removes exactly two lines, scope_note and record_count, and is otherwise pure addition after record 30. No record was inserted among the original 30, none was reordered, and no record object was reformatted.
Original 30 evidence entries unchanged: the same before-and-after canonical comparison was performed on records[0:30] of mwe-public-document-evidence.json; the two are byte-identical. The raw diff removes exactly one line, record_count, and is otherwise pure addition after entry 30.
Registry envelope: record_count 30 to 59 and scope_note replaced with the approved revised text from the master plan, which opens with the previous note verbatim and adds that inclusion records public-document selection only and establishes no internal Registry status, formal classification, conceptual priority, confirmed relation status, ontology membership or authoritative-copy identity; that some metadata values are assigned by registry policy rather than copied from the source; that per-field provenance is recorded in mwe-public-document-evidence.json; and that this registry is not a visualization-node manifest. scope, authority_ceiling, completeness, omission_meaning, inclusion_meaning, order_meaning, density_meaning, schema_version, @context, document_schema, @id, @type and name are unchanged, and each is asserted by test.
Evidence profiles for the 29 additions, exactly as specified in the appendix: inclusion inventory_declared for all 29, because each belongs to A minus R; repository_path and canonical_public_url mechanical; source_use_reference schema_const; surface_role, relation_default and boundary_references registry_policy; name source_h1 for all 29, with no name derived from MODEL_ATLAS, no Naming Declaration term used as a name source and no filename-derived normalization; classification source_declared for the sole classified addition and not_asserted for the other 28. public_surface_status and authority_ceiling are registry_policy for all 29, because none of the 29 source files carries the established public-surface header block; they were not recorded as source_declared, and no source header was normalized. Source-header normalization remains the optional S1 phase and was not performed. No mixed evidence syntax was introduced.
Evidence distributions across the 59 entries: inclusion 52 inventory_declared and 7 registry_policy; name 56 source_h1 and 3 source_declared_display_title; repository_path 59 mechanical; canonical_public_url 59 mechanical; surface_role 59 registry_policy; public_surface_status 21 source_declared and 38 registry_policy; authority_ceiling 21 source_declared and 38 registry_policy; relation_default 59 registry_policy; classification 16 source_declared and 43 not_asserted; boundary_references 59 registry_policy; source_use_reference 59 schema_const. Coverage is exactly 1:1 with the registry, in registry order, with no duplicate, missing or extraneous path.
Validator record-count derivation: the bare literal record-count comparison `len(records) != 30` was replaced with a derived check requiring the declared record_count to equal the @graph length, with the error reporting both the declared value and the actual length. One hard-coded number was not substituted for another. The success output now reports registry records 59 and evidence records 59, both derived from the parsed files.
Validator explicit path set: EXPECTED_REGISTRY_PATHS was extended to the exact 59 paths in registry order and deliberately retained as an explicit list, because it is compared by set equality in both directions and therefore catches an accidental missing approved path and an accidental additional unapproved path alike, which a runtime-derived set cannot do. The list was updated in the same commit as the 29 records so that intermediate commit validation stays coherent. A new validator check, validate_expected_paths_match_inventory, plus a test, prove that the list equals the MODEL_ATLAS File declarations union the seven known registry-only paths, so the safety net cannot drift from the declared inventory. That check confirms an inventory identity only and establishes no classification and no relation.
P2 expanded-target test migration: tests/test_public_surface_candidate_generator.py contained test_expanded_expects_fifty_nine_records, which invoked the expanded target against the live registry and relied on that registry holding 30 records. After P3 the live registry holds 59, so the live-root assumption became stale. The assertion intent was preserved, not deleted or weakened: the test now builds an isolated generator root outside both repository roots containing a clearly labelled synthetic 30-record registry fixture, a read-only copy of the surface manifest and a read-only copy of the frozen artifact, and requires the expanded target to reject it while naming the expected count 59 and the supplied count 30, with no output file produced and the frozen artifact's bytes and mtime unchanged. test_ordinary_existing_output_is_not_a_collision was migrated to the same isolated source for the same reason, so that the same-file guard is still observed without generating an expanded dataset from the live 59-record registry. A new unit-level test asserts the same contract directly at the assembly helper, where no output path is involved at all. EXPANDED_RECORD_COUNT remains 59, the collision guard was not weakened, all P2 collision, no-write and historical tests remain intact, and INTEGRATION_SHA, every INTEGRATION_EXPECTED_* constant and every pinned historical constant are unchanged. No end-to-end successful expanded generation test against the live 59-record registry was added, and no expanded artifact was created or retained; the temporary roots are removed with the class temporary directory.
Tests added for the 59-record contract: registry and evidence counts and count-to-length agreement; 59 distinct repository paths, 59 distinct @id values and existence of all 59 source files; recomputed candidate-set identity against the P3 base; the 29 appended paths equal the pre-expansion A minus R in MODEL_ATLAS declaration order; both set corrections; EXPECTED_REGISTRY_PATHS equals inventory plus the seven registry-only paths; byte-identical original 30 registry records and original 30 evidence entries against the P3 base read through Git, with an explicit precondition test that the base really is the 30-record state so the comparison cannot silently compare the file with itself; positional ordering of the original 30 and of additions 31 to 59; evidence order equal to registry order; mechanical @id and canonical URL formation for all 59; role and status cluster validity against the six attested clusters; boundary-reference cluster policy; constant source_use_reference; no new surface_role literal and no new classification literal; the 16/43 fail-closed split with each explicit literal confirmed within the first 80 source lines and each not_asserted record confirmed to carry no literal Classification line and no declared-classification field; the AUTHOR.md record; the 28-concept profile; absence of DOI, version, license, date, OSF URL, abstract, source-commit and notes fields on the additions; and that the existing JSON-LD context and document schema already cover every term, field and enum value used by the 59 records.
Schema and JSON-LD boundary: mwe-public-context.jsonld, mwe-document.schema.json and mwe-public-document-evidence.schema.json were not modified. Tests confirm the existing context defines every term used and the existing document schema permits every field and every enum value used by all 59 records, so no schema or context expansion was necessary and none was made. The evidence schema version remains 1.0 and the document schema version remains 1.0.
Checks run (all Python commands with PYTHONDONTWRITEBYTECODE=1): baseline before any edit — validator exit 0 with registry records 30 and evidence records 30, full suite 131 tests with 0 failures and 1 platform skip, zero-argument builder and builder --target historical both exit 0 and wrote nothing; after implementation — python3 scripts/validate_public_metadata.py (exit 0; registry records 59, evidence records 59), python3 -m unittest tests.test_public_document_evidence (89 tests, 0 failures), python3 -m unittest tests.test_public_surface_candidate_generator (75 tests, 0 failures, 1 platform skip), python3 -m unittest discover -s tests -p 'test_*.py' (164 tests, 0 failures, 1 platform skip), zero-argument builder (exit 0, verify-only, no write), builder --target historical (exit 0, verify-only, no write); count probes reporting registry 59 59 and evidence 59 59; classification probe reporting 16 explicit_in_file and 43 not_asserted; stat, wc -c, sha256sum, git hash-object and git diff on the tracked artifact before and after all validation (unchanged, including mtime); git diff against the P3 base on scripts/build_public_surface_authority_map.py, the historical dataset, the visualization README, mwe-public-document-evidence.schema.json, mwe-document.schema.json, mwe-public-context.jsonld, model-atlas, README.md, mwe-public-surface.json, mwe-public-surface.schema.json, public-misreading-register.json and PUBLIC_DOCUMENT_REGISTRY_POLICY.md (all empty); canonical serialization comparison of @graph[0:30] and records[0:30] between the P3 base and the working tree (byte-identical in both cases); git diff --check (clean); git ls-files --deleted (none); git status --porcelain (only the six allowed files); __pycache__ and *.pyc scan (none present); touched-prose ASCII not-equal scan on the edited JSON prose and on this worklog entry (none). All ASCII not-equal markers remaining in the touched Python files are comparison operators.
Historical identity: unchanged at 92903 bytes, SHA-256 3b1e5993a52cbce340b85472fea1ae5ea6f921cf8f7751d2d635edc7b17216ea, Git blob 2d59c4fdd07a2a9ddfad94e2e214a2d1c84912af, 30 nodes, 161 edges, 132 boundary-reference edges, 29 source-use-reference edges, 7 self-references omitted, 39 pinned reconstruction inventory entries. Bytes and mtime were unchanged across every check, including the migrated expanded-target tests.
Scope: no source concept Markdown file changed; no source-header normalization; no schema, JSON-LD or evidence-vocabulary change; no DOI, version, license, date, OSF URL, abstract, source-commit or notes enrichment; no generator change; no historical dataset change; no expanded dataset generated, retained or committed; no visualization README change; no navigation-surface change (README.md, MODEL_ATLAS.md, RELATION_MAP.md and READING_PATHS.md all unchanged, and none required an edit); no website-repository change. Phase 3A P4 was not started, and S1, P5 and P6 were not started.
Result: the registry carries the exact approved 59 records with the original 30 preserved byte-identically in their original positions, the evidence manifest covers all 59 at exact 1:1, the validator derives its record count and retains an explicit 59-path safety list anchored to the declared inventory, and the stale P2 expanded-target assumption is isolated from live registry state without weakening any P2 guarantee.
Unresolved questions: None blocking. Recorded for review: two non-blocking authorial naming notes carried over from planning and applied as approved — reality-consistency.md retains its H1 although the file's own OSF Project title is shorter, because MODEL_ATLAS uses the H1 and the existing shortened-name exception therefore does not apply; and semantic-curvature.md retains its H1 although its Naming Declaration term differs, because Naming Declaration terms are concept-naming declarations and never document-title sources.
Risks or assumptions: No MWE authority-level decision was made. No classification, relation status, candidate-to-confirmed promotion, Registry status, naming authority, public/private boundary, ontology, semantic supersession, visualization-membership or OSF-priority decision was made or implied. Registry membership does not imply visualization membership and no relation was created, promoted or confirmed. The one declared classification recorded is a transcription of an existing source line, not a classification decision. The pull request is a draft and is left unmerged for review.

### 2026-07-25 — Claude Code — phase3a-p3-review-follow-up-model-atlas-read-scope

Agent: Claude Code
Phase: Phase 3A P3 — review follow-up on pull request #28 (public-document registry expansion 30 to 59). Narrowly scoped correction of one review blocker. No redesign of the expansion and no revision of the approved P3 result.
Base and head: base unchanged at 7a5e5fe59203cc7de6e70c3d4080bbf3b92c9008; previous head 6af36d3385158d21512e30be55d78f14954737c2 on branch claude/phase-3a-registry-expansion-y4ds9b. The original five P3 commits were not amended, rebased or rewritten; this is one additional commit on the same branch and the same pull request.
Files changed: scripts/validate_public_metadata.py, tests/test_public_document_evidence.py, AGENT_WORKLOG.md (this entry). No other tracked file changed. No test module was added.
Blocker — production MODEL_ATLAS read removed: P3 added a call to validate_expected_paths_match_inventory(errors) inside validate_public_metadata(). That helper reads model-atlas/MODEL_ATLAS.md, which made MODEL_ATLAS.md a production validator source dependency. The P2 dependency-inventory builder does not enumerate MODEL_ATLAS.md, and the inventory verifier derives its expected set from that same builder enumeration, so an inventory omitting MODEL_ATLAS.md could still pass while the production validator read it. The single call was removed from validate_public_metadata(). The P2 generator and the dependency inventory were not modified, and the correction was not made by widening either.
Retained unchanged: EXPECTED_REGISTRY_PATHS (still the explicit 59-path list, verified identical to the previous head by AST comparison), REGISTRY_ONLY_PATHS, MODEL_ATLAS_FILE, MODEL_ATLAS_FILE_DECLARATION_RE and validate_expected_paths_match_inventory() itself, which remains available for direct invocation. Registry validation, evidence validation, classification validation, the derived record-count check, the success output, isolated preflight behaviour and inventory-verification behaviour are unchanged, and no source metadata changed.
Comments and docstrings corrected: the EXPECTED_REGISTRY_PATHS comment, the REGISTRY_ONLY_PATHS comment, the MODEL_ATLAS constant comment and the helper docstring no longer imply that the MODEL_ATLAS agreement check runs during every production validator invocation. They now state the division explicitly: the production validator enforces the explicit 59-path registry contract, and the test suite independently proves that the explicit list equals the MODEL_ATLAS File declarations plus the seven registry-only paths. The statement that the explicit list catches both an accidental missing approved path and an accidental added unapproved path was retained and was not weakened.
Direct union proof retained: CandidateSetTests.test_validator_reports_no_inventory_drift still calls validator.validate_expected_paths_match_inventory(errors) directly and still asserts the error list is empty, so the MODEL_ATLAS union agreement remains proved. CandidateSetTests.test_expected_registry_paths_equals_inventory_plus_registry_only continues to read MODEL_ATLAS.md in the test layer and assert the same identity independently of the helper.
Regression test added: CandidateSetTests.test_default_validator_does_not_read_model_atlas preserves validator.read_repo_text, patches it with a guarded wrapper that raises if the requested path equals validator.MODEL_ATLAS_FILE and delegates every other read to the original, then calls validator.validate_public_metadata() and requires return code 0 with stdout captured. The test was confirmed to have teeth: with the removed call temporarily reinstated it fails with "production validator must not read MODEL_ATLAS.md", and with the call removed it passes. The temporary reinstatement was reverted from a saved copy and is not part of any commit.
Contract regression test added: CandidateSetTests.test_production_validation_still_enforces_the_explicit_path_contract confirms the removal did not weaken production enforcement, by requiring validate_document_registry to report "missing expected paths" when an approved record is dropped from a copy of the registry and "unexpected registry paths" when an unapproved path is added to a copy. Both operate on deep copies; the tracked registry was not mutated.
Dependency read audit: a grep for MODEL_ATLAS_FILE and validate_expected_paths_match_inventory across the two changed files, plus an AST reachability check, confirm the helper is not called by validate_public_metadata(), is not reachable transitively from it, and that MODEL_ATLAS_FILE is not referenced anywhere in the call graph reachable from validate_public_metadata(). In the validator the constant is referenced only by the retained helper; in the tests it is referenced only by the direct union tests and the guard in the regression test.
Checks run (all Python commands with PYTHONDONTWRITEBYTECODE=1): python3 scripts/validate_public_metadata.py (exit 0; registry records 59, evidence records 59); python3 -m unittest tests.test_public_document_evidence (91 tests, 0 failures, up from 89); python3 -m unittest tests.test_public_surface_candidate_generator (75 tests, 0 failures, 1 platform skip, unchanged); python3 -m unittest discover -s tests -p 'test_*.py' (166 tests, 0 failures, 1 platform skip, up from 164); zero-argument builder and builder --target historical (both exit 0, verify-only, nothing written); verbose targeted runs of test_default_validator_does_not_read_model_atlas and test_validator_reports_no_inventory_drift (both ok); registry and evidence probes reporting registry 59 59, evidence 59 59, 16 explicit_in_file and 43 not_asserted; canonical comparison of the first 30 registry records and first 30 evidence entries against the P3 base (byte-identical); git diff of this commit against the previous head on mwe-public-documents.json, mwe-public-document-evidence.json, tests/test_public_surface_candidate_generator.py, scripts/build_public_surface_authority_map.py and the historical dataset (all empty); AST comparison of EXPECTED_REGISTRY_PATHS against the previous head (identical, 59 entries); stat, wc -c, sha256sum and git hash-object on the tracked artifact (unchanged, mtime unchanged); git diff --check (clean); git ls-files --deleted (none); git status --porcelain (only the three allowed files); __pycache__ and *.pyc scan (none present). The ASCII not-equal markers in the touched Python files are comparison operators.
Historical identity: unchanged at 92903 bytes, SHA-256 3b1e5993a52cbce340b85472fea1ae5ea6f921cf8f7751d2d635edc7b17216ea, Git blob 2d59c4fdd07a2a9ddfad94e2e214a2d1c84912af, 30 nodes, 161 edges, 132 boundary-reference edges, 29 source-use-reference edges, 7 self-references omitted, 39 pinned reconstruction inventory entries. The pinned reconstruction identity and mtime were unchanged throughout.
Scope: registry remains 59 records and evidence remains 59 entries; the original 30 registry records and the original 30 evidence entries remain byte-identical to the P3 base; the candidate set, the explicit 59-path list and the 16/43 classification split are unchanged. No registry or evidence data was altered to accommodate the test. No P2 generator change, no dependency-inventory change, no historical dataset change, no expanded dataset generated or committed, no source concept Markdown change, no schema or JSON-LD change, no navigation-surface change, no website-repository change. Phase 3A P4 was not started, and S1, P5 and P6 were not started.
Result: the MODEL_ATLAS agreement is now test-only. Production validation enforces the explicit 59-path contract and reads nothing outside the dependency set the P2 inventory enumerates, while the inventory-union proof survives intact as direct test coverage with a regression test guarding the boundary in both directions.
Unresolved questions: None blocking. Recorded for review and deliberately left unchanged as out of scope for this follow-up: read_json_at_commit() still raises SkipTest when the pinned base commit is unavailable, so the original-30 comparison would skip rather than fail in a shallow clone. That was noted as non-blocking and no change was made to it here.
Risks or assumptions: No MWE authority-level decision was made. No classification, relation status, candidate-to-confirmed promotion, Registry status, naming authority, public/private boundary, ontology, semantic supersession, visualization-membership or OSF-priority decision was made or implied. No conceptual file was rewritten. Pull request #28 remains a draft and unmerged for review.

### 2026-07-25 — Claude Code — phase3a-p5-expanded-adjacency-dataset
Agent: Claude Code
Phase: Phase 3A P5 — expanded visualization dataset planning and generation, controlled by PHASE3ACtaskpackages.md task package C-P5. P4 was accepted with the 59-record registry. S1 was not executed, P6 was not started, and no website-repository file was touched.
Base and pinned source: expected P5 base 933274af9693d6d1d9fac36819aafdf56f9ab81d; resolved origin/main identical at 933274af9693d6d1d9fac36819aafdf56f9ab81d with a clean working tree, so no divergence handling was required. The pinned generation source commit recorded in the manifest and dataset is 933274af9693d6d1d9fac36819aafdf56f9ab81d. Branch: claude/phase-3a-p5-visualization-k7jvla (session-designated branch used in place of the fallback name).
S1 status: skipped as approved. No source header was normalized, no source Markdown was modified, and evidence provenance was not changed.
Files changed: added visualizations/public-surface-adjacency-map/data.json, visualizations/public-surface-adjacency-map/visualization-manifest.json, visualizations/public-surface-adjacency-map/visualization-manifest.schema.json, visualizations/public-surface-adjacency-map/README.md and tests/test_public_surface_adjacency_map.py; modified scripts/build_public_surface_authority_map.py, tests/test_public_surface_candidate_generator.py and AGENT_WORKLOG.md (this entry). No other tracked file changed.
Output directory: visualizations/public-surface-adjacency-map/ exactly, containing only the four approved product files. No expanded output was written under visualizations/public-surface-authority-map/, and no tracked dependency-inventory file was added; expanded dependency provenance is enumerated in memory and in temporary test output only.
Manifest contract: an independent visualization manifest with authority ceiling visualization_membership_and_rendering_policy_only, described by a JSON Schema Draft 2020-12 file with additionalProperties false at every object level, every top-level and record field required, closed enums, unique paths, minItems and maxItems both 59, and record_count pinned to 59. It references but is not embedded into mwe-public-documents.json and does not extend the registry contract. It carries no generated timestamp.
Manifest identity: 59 records, exactly 1:1 with the registry in registry order; byte length 18550; SHA-256 b1db120e3bbaef0d35ff95fa79de3231f9b4f8f183b2a60b7f8729c459112d12; Git blob 1ce71b0abd2e8485cf807db0a9ba0898b1f23e55.
Membership distribution: included 59, excluded 0, pending 0.
Role distribution: concept 49, orientation 2, boundary 7, anchor 1, total 59, mapped mechanically from the existing registry surface_role with concept_node to concept, repository_orientation to orientation, public_anchor to anchor and boundary_document, interpretation_guide and source_use_guide to boundary. README.md and AUTHOR.md are the two orientation records and public-anchors/ai-training-boundary-statement.md is the anchor record. No further visualization role was introduced.
Label decision: display_label_source is registry_name for all 59 records, and every displayed label equals the registry name byte-for-byte. No label was derived from a filename, MODEL_ATLAS heading, Naming Declaration, DOI title or shortened form.
Grouping decision: concept records use grouping_source model_atlas_field and non-concept records use grouping_source visualization_role. All 49 concepts resolve to exactly one public navigation field read from the entry's own declared File line under its field heading in model-atlas/MODEL_ATLAS.md; none resolved to zero or to more than one. Field distribution: Boundary / Representation 10, Semantic Field Foundations 10, Responsibility / Benefit-Burden / Cost 7, AI-Readable Interface / Externalization 6, Coherence / Circulation / Collapse Risk 6, Proxy / Legibility / Provenance 6, Constraint / Residue / Capability Shift 4. MODEL_ATLAS was not edited to make parsing easier, and the dataset and README both state that field grouping is a navigation grouping and not a formal classification, ontology, hierarchy, priority order, confirmed relation or internal Registry assignment.
Source-named adjacency: raw 180 directed, retained concept-to-concept 180, excluded for a non-concept endpoint 0, default visible true, directed as written, display label Source-declared adjacency. Extraction reads only the declared adjacency syntax, namely a top-level list item inside the document's own Relationship to Adjacent Models or Relationship to Adjacent Public Frameworks section whose leading element is a Markdown link carrying an explicit repository-relative path. 28 files carry an adjacency heading, matching the planning measurement, and 18 registered documents declare adjacency in that syntax. Planning recorded 189 raw directed edges; the recomputed value from the pinned source under the strict declared-syntax rule is 180, and the recomputed value was kept rather than forced. The 9 to 10 difference is accounted for by inline prose links inside adjacency sections, which are deliberately not treated as adjacency. Files whose adjacency section uses only plain-text names, fenced comparison blocks or prose contribute no edge, because those forms declare no path resolvable without guessing.
Navigation adjacency: raw 201 directed, retained concept-to-concept 194, excluded for a non-concept endpoint 7, default visible false, directed as written, display label Provisional navigation adjacency, read only from the Adjacent entries column of model-atlas/RELATION_MAP.md. The recomputed raw values reproduce the planning measurement exactly at 201 directed, 155 undirected pairs and 46 reciprocal pairs.
Edge separation: the two classes are preserved separately and never merged; direction is preserved as written; no reciprocity and no reverse edge is inferred; duplicate identical directed edges are rejected within a class only and never across classes; identical directed pairs occurring in both classes remain two distinct edges. No edge was resolved by guessing: an endpoint that cannot be mapped to exactly one of the 59 registry paths by an explicitly written path stops the run with EXPANDED_EDGE_ENDPOINT_UNRESOLVED, and none occurred at the pinned source. No relation is described as confirmed and no user_confirmed_relation exists.
Governance strategy: governance_reference and source_use_reference produce no rendered edges, no governance overlay was implemented, no governance toggle exists, and neither class contributes to layout, degree, rank or centrality. The projected 250 governance_reference, 58 source_use_reference and 308 combined edges are absent from the dataset's semantic edge arrays. The exclusion is stated once at product level in the dataset boundary statements and in the README, and the registry's boundary_references were not weakened, removed or reinterpreted.
Relation-evidence ceiling distribution: source_named_adjacency 18, navigation_adjacency 31, none 10. All 10 non-concept records are none. The field records the strongest approved display evidence available per record and is not a classification, hierarchy, confirmed relation, priority order or ontology statement.
Dataset identity: byte length 202303; SHA-256 370cde8431641a4d5118e72379564deea0012cef42e49cf6542d319c8f46da69; Git blob 161501533c2378a24aac666252577974fdee9acc; 59 nodes; 374 edges as 180 source-named and 194 navigation; 49 concepts with semantic_layout_participation true and 10 non-concept records with semantic_layout_participation false in fixed orientation and boundary bands; source commit 933274af9693d6d1d9fac36819aafdf56f9ab81d; 202303 bytes against the 262144-byte consumer ceiling, which the generator enforces and refuses to exceed. The dataset publishes no rank, centrality, authority score, importance, priority, canonicality, confidence, relation strength, confirmed flag, degree metric or timestamp.
Deterministic rebuild proof: generated twice to two temporary paths outside the repository and compared byte-for-byte before the tracked file was written; both were identical. The tracked dataset was then generated, and a third rebuild to a further external temporary path was compared byte-for-byte with the tracked file and was identical. Byte length, SHA-256 and Git blob were recomputed independently after generation rather than asserted in advance, and the test module's tracked identity constants were pinned only after the tracked files existed.
Generator changes: scripts/build_public_surface_authority_map.py gained the expanded adjacency target, a new required --visualization-manifest option for --target expanded, the MODEL_ATLAS and RELATION_MAP parsers, the source-named adjacency parser, the visualization-role mapping, the relation-evidence-ceiling computation and the consumer-ceiling guard. New stable failure tokens: EXPANDED_TARGET_REQUIRES_VISUALIZATION_MANIFEST, EXPANDED_VISUALIZATION_MANIFEST_INVALID, EXPANDED_GROUPING_UNRESOLVED, EXPANDED_EDGE_ENDPOINT_UNRESOLVED and EXPANDED_DATA_EXCEEDS_CONSUMER_CEILING. All P2 historical behaviour is preserved exactly: zero arguments and --target historical remain verify-only and write nothing, and the historical constants, pinned reconstruction identity, existing failure tokens and path-collision protections are unchanged. Collision checking still runs before the manifest requirement, before registry processing, before edge processing, before parent creation and before any write, proved by a test that supplies the historical path with no manifest and receives HISTORICAL_OUTPUT_PATH_COLLISION rather than the manifest token. The live registry is never the visualization-membership authority; the manifest is read and never written; unreadable source or manifest input fails closed.
Historical isolation: the pinned 39-entry historical dependency reconstruction was not widened or altered, and expanded dependency enumeration does not contaminate it. A test traces every path opened during both production historical verifications and asserts that MODEL_ATLAS.md, RELATION_MAP.md, the visualization manifest, the manifest schema and the expanded dataset are never read, while the historical artifact is.
Tests: added tests/test_public_surface_adjacency_map.py with 83 tests covering manifest schema identity, manifest identity and 1:1 registry-order coverage, grouping, edge extraction including unresolved-target, out-of-registry, duplicate, no-reverse-edge and non-concept-endpoint cases, dataset identity, deterministic rebuild, expanded and historical isolation, and the compatibility guard. tests/test_public_surface_candidate_generator.py was modified only where the new required flag made it necessary: the isolated 30-record fixture now also writes a syntactically valid placeholder manifest, and the two expanded-target tests pass --visualization-manifest so that the registry record-count guard remains what stops the run. INTEGRATION_SHA, every INTEGRATION_EXPECTED_ constant, every pinned historical constant and EXPANDED_RECORD_COUNT are unchanged, and all existing P2 and P3 collision, no-write, symlink, hard-link, traversal, reconstruction and inventory tests remain intact and passing.
Checks run (all Python commands with PYTHONDONTWRITEBYTECODE=1): baseline before any edit — validator exit 0 with registry records 59 and evidence records 59, tests.test_public_document_evidence 91 tests, tests.test_public_surface_candidate_generator 75 tests with 1 platform skip, full discovery 166 tests with 1 platform skip, zero-argument builder and builder --target historical both exit 0 and wrote nothing; after implementation — validator exit 0 with registry records 59 and evidence records 59, tests.test_public_document_evidence 91 tests, tests.test_public_surface_candidate_generator 75 tests with 1 platform skip, tests.test_public_surface_adjacency_map 83 tests, full discovery 249 tests with 1 platform skip and 0 failures, zero-argument builder and builder --target historical both exit 0 and wrote nothing; manifest and dataset probes reporting manifest 59 59, membership included 59, roles concept 49 orientation 2 boundary 7 anchor 1, nodes 59 and dataset bytes 202303; wc -c, sha256sum and git hash-object on the tracked dataset and manifest; stat, wc -c, sha256sum, git hash-object and st_mtime_ns on the historical artifact before and after all generation and tests, compared against a copy stored outside the repository; git diff against the P5 base on mwe-public-documents.json, mwe-public-document-evidence.json, visualizations/public-surface-authority-map, model-atlas, README.md, AUTHOR.md and all Markdown except the worklog and the new product README (all empty); git diff --check (clean); git ls-files --deleted (none); __pycache__ and *.pyc scan (none present); literal ASCII not-equal scan of the new product README (none). The ASCII not-equal markers remaining in the touched Python files are comparison operators.
Historical identity: unchanged at 92903 bytes, SHA-256 3b1e5993a52cbce340b85472fea1ae5ea6f921cf8f7751d2d635edc7b17216ea, Git blob 2d59c4fdd07a2a9ddfad94e2e214a2d1c84912af, 30 nodes, 161 edges, 39 pinned reconstruction inventory entries. Bytes and st_mtime_ns were byte-for-byte unchanged across every check including all expanded generation runs.
Compatibility guard: registry remains 59 and evidence remains 59; the classification split remains 16 explicit_in_file and 43 not_asserted; no new registry surface_role literal and no new publicly_declared_classification literal appeared; the original 30 records are unchanged; no source Markdown changed; manifest roles do not alter registry roles; visualization membership does not imply Registry status; visualization role does not imply classification; MODEL_ATLAS grouping does not imply ontology; and adjacency does not imply a confirmed relation.
Scope: no historical dataset change; no historical visualization README change; no expanded output under the historical directory; no registry, evidence, schema, JSON-LD or policy change; no navigation-surface change to MODEL_ATLAS.md, RELATION_MAP.md or READING_PATHS.md; no public-anchor change; no website-repository change. S1 was not performed, P6 was not started, and no page was published.
Result: a separate expanded adjacency product representing all 59 registered records, with 49 concepts in semantic layout and 10 records in fixed orientation and boundary bands, two separately preserved directed edge classes, deterministic byte-identical reconstruction, and the frozen 30-node product left untouched and independently verifiable.
Unresolved questions: For user review, not blocking. First, the source-named adjacency rule was implemented strictly, so an adjacency section that names adjacent models only in prose, in a fenced comparison block, or as plain-text bullets without a link contributes no edge; 10 files with adjacency headings are silent for this reason, and whether any of those forms should later be treated as declared adjacency is an authorial decision that was not made here. Second, the recomputed raw source-named count is 180 against the planning figure of 189, for the reason recorded above; the recomputed value was kept as instructed and the planning figure was not forced. Third, relation_evidence_ceiling was assigned from retained display evidence, so a concept whose adjacency section exists but yields no rendered edge falls to its navigation evidence rather than to source_named_adjacency.
Risks or assumptions: No MWE authority-level decision was made. No classification, relation status, candidate-to-confirmed promotion, Registry status, naming authority, public/private boundary, ontology, hierarchy, priority, semantic supersession or OSF-priority decision was made or implied. No relation was created, promoted or confirmed, and no visualization was published. Visualization membership and rendering policy are the only decisions the manifest encodes, and both were fixed by the approved user decisions rather than chosen here. The pull request is a draft and is left unmerged for review.

### 2026-07-25 — Claude Code — phase3a-p5-review-follow-up-machine-readable-adjacency
Agent: Claude Code
Phase: Phase 3A P5 — review follow-up on pull request #29. Narrowly scoped correction of four review blockers under two subsequent authorial decisions. The expansion itself was not restarted or redesigned.
Base and head: base unchanged at 933274af9693d6d1d9fac36819aafdf56f9ab81d; previous head df38c22a18517e8db9a20e4fb9c2c815cb84430e on branch claude/phase-3a-p5-visualization-k7jvla. The original five P5 commits were not amended, rebased, squashed or rewritten; this adds three commits to the same branch and the same pull request.
Files changed: scripts/build_public_surface_authority_map.py, tests/test_public_surface_adjacency_map.py, visualizations/public-surface-adjacency-map/data.json, visualizations/public-surface-adjacency-map/visualization-manifest.json, visualizations/public-surface-adjacency-map/README.md, AGENT_WORKLOG.md (this entry). No other tracked file changed.
Authorial decision 1 — source-named adjacency contract: source_named_adjacency is repository-relative Markdown document links written inside the source document's formal adjacency section. This is a machine-readable edge contract and does not attempt to interpret every human-readable concept name in the section.
Contract widened as approved: every Markdown link inside the formal section now counts, whether it leads a bullet, appears later in a bullet, appears in a sentence, appears in a table row, or appears inside a fenced block. The previous implementation accepted only a link at the start of a top-level bullet; that narrowing is removed. Only the href is resolved and the visible link label is never consulted, so no edge can depend on a concept name.
Explicitly not declarations, creating no edge and no unresolved candidate: bare concept names, plain-text bullets without links, fenced comparison-block labels without links, descriptive prose, definitions using =, labels such as ownership, visibility or accountability, short conceptual names differing from registry names, Naming Declaration terms, and any inferred alias, prefix or fuzzy match. No short-name alias map, registry-name alias map, manually curated relation map, source normalization or inferred name-to-path mapping was created.
Ignored without failing: external URLs, DOI links, OSF links, fragment-only anchors, non-Markdown-file links, and links outside the formal section. Counted separately in the temporary audit.
Fail-closed internal links: a link that is a repository-relative Markdown document reference but escapes the repository, resolves to no file, resolves outside the 59-record registry, or cannot be normalized mechanically stops the run with EXPANDED_SOURCE_ADJACENCY_UNRESOLVED, reporting source file, one-based line, exact href, exact source line and reason. Zero occurred at the pinned source.
Multiple formal sections in one document fail closed rather than silently using the first. Zero occurred at the pinned source.
Authorial decision 2 — same-section evidence consolidation: within one document's single formal adjacency section, the same source path, resolved target path and edge class produce exactly one unique directed edge, with all evidence occurrences retained in the temporary audit as source, target, declaration_count, declaration_lines and declaration_hrefs. At the pinned source this occurs exactly once, in responsibility-alignment-diagnostics.md at lines 446 and 450, both resolving to responsibility-alignment-model.md, one in explanatory prose and one in the structured list; declaration_count is 2 and the dataset contains one edge. No source Markdown was edited.
Still failing closed with EXPANDED_DUPLICATE_DIRECTED_EDGE: duplicate navigation_adjacency directed pairs in RELATION_MAP.md, the same source-named pair reached from multiple formal sections, multiple formal adjacency sections in one document, duplicates from incompatible evidence scopes, and any duplicate not safely attributable to repeated evidence for one edge. The failure reports first and duplicate locations. Cross-class identical pairs remain two separate edges and are never deduplicated across classes.
Source-link audit at the pinned source: 28 documents with a formal adjacency section; 0 with more than one; 190 Markdown links inside those sections; 190 accepted internal registered link declarations; 0 ignored external/fragment/non-document links; 0 self-references omitted; 1 same-section repeated evidence occurrence; 0 unresolved internal Markdown links; 189 unique source-named directed edges; 189 retained concept-to-concept; 0 excluded non-concept endpoint edges. Declarations minus repeated evidence equals unique edges. The previous 180 was not retained and the planning figures 189 and 190 were not forced; every value was recomputed.
false-legibility.md: formal adjacency section present at lines 202 to 236; bare conceptual names present in a fenced comparison block including Premature Coherence, Reality Consistency, Semantic Curvature and Semantic Virology; zero Markdown links; therefore no source-named edge created from bare names and no unresolved error created from bare names; navigation adjacency remains available for it independently. Covered by a dedicated regression test.
Navigation adjacency: unchanged at 201 raw unique directed, 194 retained concept-to-concept, 7 excluded non-concept endpoint, 0 duplicates, 0 unresolved; reciprocal pairs remain two directed edges.
Relation-evidence ceiling corrected: computed from accepted, non-self Markdown-link declarations before non-concept rendering filtration rather than from the final rendered edge list, so a record retains source-level evidence when its valid targets are filtered from rendering. Recomputed distribution is 20 source_named_adjacency, 29 navigation_adjacency and 10 none, replacing the previous 18/31/10. Two manifest ceiling values changed; no other manifest record or field was altered.
Expanded dependency provenance: a separate expanded-target in-memory inventory records every file actually read, hashed or existence-checked, through a single recording read layer so enumeration equals actual reads by construction. It covers the registry, the visualization manifest, the visualization-manifest schema, MODEL_ATLAS.md, RELATION_MAP.md and all 59 registered source documents audited for adjacency: 64 dependencies, aggregate SHA-256 a89f1aefd341778f89e7b1e810ed760ddb7de7ff30564bda93fdaeb7a451918f, each entry carrying path, byte_length, sha256, git_blob_sha1 and sorted read_purposes from a closed vocabulary. A bidirectional actual-read trace test proves enumerated equals actual in both directions, failing for an actual read absent from the inventory and for an inventory entry never read. No tracked dependency-inventory file was added and the inventory is not embedded in data.json; only the count and aggregate are reported in README and here.
Historical isolation preserved: the pinned 39-entry historical reconstruction and collect_read_purposes were not widened or modified; tests confirm the historical enumeration still excludes MODEL_ATLAS.md, RELATION_MAP.md and the visualization manifest, that historical verification opens none of the expanded-only inputs, and that the pinned inventory count remains 39.
Fail-closed source reads: EXPANDED_SOURCE_INPUT_UNREADABLE is used consistently for path escape, missing file, non-regular file, unreadable file, invalid UTF-8 and other source read failures, including the path and reason, and is applied to the registry, MODEL_ATLAS, RELATION_MAP, registered source documents and the manifest schema. Manifest and manifest-schema contract failures continue to use EXPANDED_VISUALIZATION_MANIFEST_INVALID.
Production manifest schema validation: production generation now resolves $schema relative to the manifest, requires a readable regular Draft 2020-12 schema file, and validates the manifest against type, const, enum, required, additionalProperties, minItems, maxItems, uniqueItems, minLength, pattern, items and properties, reporting a precise schema path. The small schema-instance checker was moved from the test module into production code and the test module now delegates to it, so one implementation is enforced rather than two that could diverge. The tracked manifest schema was not modified and remains byte-identical to the previous head.
Edge output fields: relation_status and authority_ceiling are retained as the planning contract allows, with relation_status always the evidence class and never confirmed, and authority_ceiling always navigation_only. is_confirmed_relation was removed from the edge-class declarations. One transform note was renamed from adjacency_implies_confirmed_relation to adjacency_implies_formal_relation so that no published key contains confirmed; the absence of confirmed relations remains stated in boundary prose and by the absence of user_confirmed_relation. A recursive test rejects any dataset key containing confirm and any key equal to a rank, centrality, degree or formal-relation-status name.
Dataset identity: 206617 bytes; SHA-256 0b763eb78fea5c53364609ecc5d7019422c54b950d32f29f79ad37f24f1637b7; Git blob 3077568edeeb0d6a769899a1a3cf79c3f9152f83; 59 nodes; 383 edges as 189 source-named and 194 navigation; 49 concepts in semantic layout and 10 in fixed bands; source commit 933274af9693d6d1d9fac36819aafdf56f9ab81d; within the 262144-byte consumer ceiling.
Manifest identity: 18554 bytes; SHA-256 b6ea211e265631b984f0e9ea53fb7301f3fd0986dbdaa2a9d349c0524591d0fe; Git blob 159ac950abf2172bcdd2cc420afde63578140eb8; 59 records, 1:1 with the registry in registry order; membership included 59, excluded 0, pending 0.
Deterministic rebuild proof: generated to two external temporary paths and compared byte-for-byte before the tracked file was written, both identical; the tracked file was then generated and a third external rebuild compared byte-for-byte with it, identical. All identity constants were recomputed after generation rather than asserted in advance.
Checks run (all Python commands with PYTHONDONTWRITEBYTECODE=1): python3 scripts/validate_public_metadata.py (exit 0; registry records 59, evidence records 59); python3 -m unittest tests.test_public_document_evidence (91 tests, 0 failures); python3 -m unittest tests.test_public_surface_candidate_generator (75 tests, 0 failures, 1 platform skip, unchanged); python3 -m unittest tests.test_public_surface_adjacency_map (135 tests, 0 failures, up from 83); python3 -m unittest discover -s tests -p 'test_*.py' (301 tests, 0 failures, 1 platform skip, up from 249); zero-argument builder and builder --target historical (both exit 0, verify-only, nothing written); the tracked expanded generation command; three deterministic rebuilds to external temporary paths; stat, wc -c, sha256sum, git hash-object and st_mtime_ns on the historical artifact before and after all generation and tests, compared against a copy held outside the repository; git diff against the P5 base on the registry, evidence manifest, historical visualization directory, model-atlas, README.md, AUTHOR.md and all Markdown except the worklog and the product README (all empty); git diff against the previous P5 head on the manifest schema, candidate-generator tests, historical visualization directory, registry, evidence manifest and model-atlas (all empty); git diff --check (clean); git ls-files --deleted (none); __pycache__ and *.pyc scan (none present); literal ASCII not-equal scan of the product README (none). The ASCII not-equal markers in the touched Python files are comparison operators.
Historical identity: unchanged at 92903 bytes, SHA-256 3b1e5993a52cbce340b85472fea1ae5ea6f921cf8f7751d2d635edc7b17216ea, Git blob 2d59c4fdd07a2a9ddfad94e2e214a2d1c84912af, 30 nodes, 161 edges, 39 pinned reconstruction inventory entries. Bytes and st_mtime_ns were byte-for-byte unchanged across every check, including every expanded generation run and every fail-closed test.
Scope: registry remains 59 and evidence remains 59; the classification split remains 16 explicit_in_file and 43 not_asserted; the original 30 records are unchanged; no new surface_role literal and no new declared-classification literal; no source Markdown changed; MODEL_ATLAS.md, RELATION_MAP.md and READING_PATHS.md unchanged; the manifest schema and the candidate-generator tests unchanged; no historical dataset, README, app.js, index.html or styles.css change; no expanded output under the historical directory; no schema, JSON-LD or policy change; no public-anchor change; no website-repository change. S1 was not performed and P6 was not started.
Result: source_named_adjacency now matches the approved machine-readable contract with zero unresolved declarations and zero structurally conflicting duplicates, expanded provenance is enumerated and proved equal to actual reads, source inputs and the manifest contract fail closed under stable tokens, and the frozen 30-node product remains untouched and independently verifiable.
Unresolved questions: None blocking. Recorded for review: the 52 distinct bare-label phrases and short conceptual names that appear inside formal adjacency sections but map to no registered record remain unencoded by design under the approved contract; converting any of them into machine edges would require a separate authorial alias or relation mapping, which was not created and is not implied by their omission.
Risks or assumptions: No MWE authority-level decision was made. No classification, relation status, candidate-to-confirmed promotion, Registry status, naming authority, public/private boundary, ontology, hierarchy, priority, semantic supersession or OSF-priority decision was made or implied. The two contract decisions applied here were supplied by the user and were implemented as written. Same-section evidence consolidation merges repeated evidence for one adjacency and creates no new relation. Pull request #29 remains a draft and unmerged for review.
